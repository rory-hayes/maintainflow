diff --git a/node_modules/postgres/cf/src/connection.js b/node_modules/postgres/cf/src/connection.js
index 8e79170..371f900 100644
--- a/node_modules/postgres/cf/src/connection.js
+++ b/node_modules/postgres/cf/src/connection.js
@@ -174,9 +174,15 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       build(q)
-      return write(toBuffer(q))
+      const written = write(toBuffer(q))
+      // A transaction BEGIN must reserve the connection once its bytes have
+      // been accepted, even at a pipeline boundary or under socket backpressure.
+      if (q.options.onexecute) {
+        q.options.onexecute(connection)
+        return false
+      }
+      return written
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
@@ -298,4 +304,4 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   /* c8 ignore next 3 */
   function drain() {
-    !query && onopen(connection)
+    !query && !connection.reserved && onopen(connection)
   }
diff --git a/node_modules/postgres/cf/src/index.js b/node_modules/postgres/cf/src/index.js
index ffbe7ae..376373d 100644
--- a/node_modules/postgres/cf/src/index.js
+++ b/node_modules/postgres/cf/src/index.js
@@ -340,3 +340,10 @@ function Postgres(a, b) {
+    // BEGIN queries must wait for a free connection so they can reserve it
+    // before any unrelated work is dispatched to that connection.
+    if (query.options.onexecute) {
+      queries.push(query)
+      return
+    }
+
     busy.length
       ? go(busy.shift(), query)
       : queries.push(query)
diff --git a/node_modules/postgres/cjs/src/connection.js b/node_modules/postgres/cjs/src/connection.js
index 07f6716..8f94113 100644
--- a/node_modules/postgres/cjs/src/connection.js
+++ b/node_modules/postgres/cjs/src/connection.js
@@ -172,9 +172,15 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       build(q)
-      return write(toBuffer(q))
+      const written = write(toBuffer(q))
+      // A transaction BEGIN must reserve the connection once its bytes have
+      // been accepted, even at a pipeline boundary or under socket backpressure.
+      if (q.options.onexecute) {
+        q.options.onexecute(connection)
+        return false
+      }
+      return written
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
@@ -296,4 +302,4 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   /* c8 ignore next 3 */
   function drain() {
-    !query && onopen(connection)
+    !query && !connection.reserved && onopen(connection)
   }
diff --git a/node_modules/postgres/cjs/src/index.js b/node_modules/postgres/cjs/src/index.js
index f09c61c..f02addf 100644
--- a/node_modules/postgres/cjs/src/index.js
+++ b/node_modules/postgres/cjs/src/index.js
@@ -339,3 +339,10 @@ function Postgres(a, b) {
+    // BEGIN queries must wait for a free connection so they can reserve it
+    // before any unrelated work is dispatched to that connection.
+    if (query.options.onexecute) {
+      queries.push(query)
+      return
+    }
+
     busy.length
       ? go(busy.shift(), query)
       : queries.push(query)
diff --git a/node_modules/postgres/src/connection.js b/node_modules/postgres/src/connection.js
index 1b1cccd..0f7a556 100644
--- a/node_modules/postgres/src/connection.js
+++ b/node_modules/postgres/src/connection.js
@@ -172,9 +172,15 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       build(q)
-      return write(toBuffer(q))
+      const written = write(toBuffer(q))
+      // A transaction BEGIN must reserve the connection once its bytes have
+      // been accepted, even at a pipeline boundary or under socket backpressure.
+      if (q.options.onexecute) {
+        q.options.onexecute(connection)
+        return false
+      }
+      return written
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
@@ -296,4 +302,4 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   /* c8 ignore next 3 */
   function drain() {
-    !query && onopen(connection)
+    !query && !connection.reserved && onopen(connection)
   }
diff --git a/node_modules/postgres/src/index.js b/node_modules/postgres/src/index.js
index c7fba3d..1d2046c 100644
--- a/node_modules/postgres/src/index.js
+++ b/node_modules/postgres/src/index.js
@@ -339,3 +339,10 @@ function Postgres(a, b) {
+    // BEGIN queries must wait for a free connection so they can reserve it
+    // before any unrelated work is dispatched to that connection.
+    if (query.options.onexecute) {
+      queries.push(query)
+      return
+    }
+
     busy.length
       ? go(busy.shift(), query)
       : queries.push(query)
diff --git a/node_modules/postgres/types/index.d.ts b/node_modules/postgres/types/index.d.ts
index 13c3432..5a70047 100644
--- a/node_modules/postgres/types/index.d.ts
+++ b/node_modules/postgres/types/index.d.ts
@@ -50,6 +50,13 @@ interface BaseOptions<T extends Record<string, postgres.PostgresType>> {
    * @default 10
    */
   max: number;
+  /**
+   * Maximum number of queries pipelined behind the active query. Use 0 to
+   * serialize queries for transaction-mode poolers that do not support
+   * pipelining.
+   * @default 100
+   */
+  max_pipeline: number;
   /**
    * Idle connection timeout in seconds
    * @default process.env['PGIDLE_TIMEOUT']
