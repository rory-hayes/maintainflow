# MaintainCode Ads release status — 8 September 2026

## Current deployment and database

The replacement is live at `https://maintainflow.io`, using the owner-selected existing Supabase project `dhbevbimoajwkuzcunwz` in Frankfurt. No tables were deleted. The empty 001–018 baseline was upgraded through migration027; all 27 migration records, restricted runtime permissions, site registry isolation and maintenance/recipient checks passed on the actual verified-TLS pooler connection. The former bypass role is retired. Unrelated projects were preserved.

The three explicitly approved production environment replacements are saved. Read-back verified all 23 settings, production-only, with the two public Auth values matching the reused project. The database credential remains sensitive and server-side. The public domain probe at 20:30:52 UTC verified exact revision `28ecfd3988638c28f6778277b19a7977fa165e98`, authenticated readiness, app/tracker routes, retired legacy APIs and the www redirect. Deployment `dpl_JBbiUeL7fhwgFFC837ho3oE3ui1V` is READY in `fra1`.

That main revision passed [CI](https://github.com/rory-hayes/maintainflow/actions/runs/34275021619) and [CodeQL](https://github.com/rory-hayes/maintainflow/actions/runs/34275020803): 1,261 application tests and 85 PostgreSQL tests, plus live contract, sample/persistent browser, load and container/readiness checks. The contract refresh was independently compared across the complete document: one endpoint-summary wording change, with all 73 operations and executable schemas unchanged.

## Fresh customer acceptance on the reused backend

| Journey | Direct evidence | Remaining boundary |
| --- | --- | --- |
| Signup and confirmation | Normal signup/Terms, actual base-address Gmail confirmation with SPF/DKIM/DMARC pass, received-link confirmation and authenticated session after reopening browser | One authorized owner; unrelated-user acceptance and actual token-expiry refresh remain open |
| Sign-out and recovery | Normal sign-out, recovery request and actual recovery email with SPF/DKIM/DMARC pass; received link opens the password-update form | Final password submission awaits the separately requested user instruction; old/new-password checks are pending |
| Workspace and website | Normal Starter trial creation, one owned HTTPS HTML fixture site, owner access and persistence across reload | Controlled acceptance workspace; no paid plan or external customer installation |
| Health report | Health-only consent saved for the exact approved base address; current recipient validated; one workspace-scoped run accepted one report; actual Gmail receipt passed SPF/DKIM/DMARC | Manual execution only; weekly delivery needs its genuine seven-day interval |
| Duplicate suppression | Repeating the unchanged workspace-scoped check accepted zero additional emails | No global maintenance call or provider sync |
| Received unsubscribe link | GET rendered confirmation and left the subscription active; native form POST reproduced HTTP400 because `no-referrer` makes its Origin null | This change fixes the header while preserving strict origin checks; deployed same-link opt-out retest is required |
| Desktop/mobile app | All eight tabs at 1440×1000 and 390×844 loaded with no page overflow, JavaScript exceptions or failed app requests; mobile close returned keyboard focus | Automated audit is not physical-device or full accessibility certification |

The rendered audit found low-contrast supporting text and three mobile table regions without keyboard access. This change strengthens text contrast, names/focuses scroll regions and corrects customer-facing billing/sample copy. The unsubscribe fix uses an origin-only referrer policy so its token-bearing URL is not forwarded, while a normal same-origin form retains its browser Origin. Code checks and a fresh deployed retest remain separate evidence.

Detailed sanitized evidence is in the local `/tmp/maintaincode-reuse-*` acceptance artifacts. The [production audit](maintaincode-production-audit.md) preserves earlier findings; its older mvz observations do not establish current dhbe acceptance. Tracker capture, export reconciliation and selected-site deletion on this backend are still in progress.

## Remaining external gates

- Real authorized HubSpot form-field delivery and lifecycle/deal reconciliation; real OpenAI Ads inventory/cost sync. Controlled tags and schema checks do not prove either provider.
- Stripe access, new prices/webhook and test checkout, entitlement and portal lifecycle. No charge or paid subscription has been created.
- Actual scheduled maintenance and first weekly report; manual processing does not establish cron execution.
- Password-change completion, token-expiry refresh and two unrelated authenticated identities exercising access boundaries.
- Production backup destination/retention, tested hosted restore/Auth continuity and external customer value. Local rollback/recovery drills are narrower evidence.

Pro with Micro compute is the recommended starting Supabase configuration, approximately US$25/month for one active project in a Pro organization. Additional active projects add compute cost; no upgrade or add-on was purchased. Current target billing/backup history still needs direct dashboard confirmation. See [Supabase pricing](https://supabase.com/pricing), [compute billing](https://supabase.com/docs/guides/platform/manage-your-usage/compute) and [backup limits](https://supabase.com/docs/guides/platform/backups). Keep unrelated projects out of any plan/resource change.

## Historical acceptance before the backend became unavailable

This is an evidence ledger, not a claim that every customer/provider journey is accepted.

Current acceptance was exercised on live revision `e86c4fddcb32eb075e9c23b461e630a543b5b38e`. This follow-up corrects the shared loading fallback to MaintainCode Ads. Its production build, scoped lint and diff checks passed; no behavior change was introduced.

Portable record: [sanitized live acceptance evidence](evidence/2026-09-07-live-acceptance.json). This committed record consolidates the observed acceptance checks and their limits; temporary paths below identify detailed local evidence.

## Live and verified

- Target repository `rory-hayes/maintainflow` was fast-forwarded from `b20f9f7fed2232381cbdeca2efbc65d8a6d477aa` to the replacement `da088249a320f489ea378c6bf2eadeab5385dd1d`. PR #11 is merged. The previous source and production deployment remain available for rollback.
- `https://maintainflow.io` serves MaintainCode Ads. Public probes without a Vercel bypass verified the exact compiled release, app rendering, tracker CORS, retired API responses and the `www` redirect. Authenticated application readiness returned 200; unauthenticated readiness returned 401.
- The initial Git-triggered cutover deployment was `dpl_Fos7jVLJUAvyaSHht93M7F5dntFX`, following verified candidate `dpl_8nDMsD1vTM31U9t74dxwxDtg9hBR`. Both apex and www were verified against the Git deployment. The default function region is Dublin (`dub1`), beside the new Ireland database; Fluid/build settings and the Hobby plan were preserved.
- New Supabase project `mvzspyhwoqzcygekridy` has all 26 migrations. After the owner's explicit approval, migration026 was applied from its reviewed guarded SQL. Fresh hosted checks passed TLS, the restricted non-bypass runtime role, tenant persistence/isolation, encrypted credentials, maintenance queue permissions and the boolean recipient validator. Synthetic fixtures were rolled back and their absence checked.
- All 23 expected Vercel production settings are present. Private database, encryption, job and Resend values are sensitive settings; public settings and active key ID are encrypted settings. Local test mode and advertiser mutations remain disabled. Credentials are absent from the source upload, which contained only 550 tracked files.
- Supabase confirmation/recovery templates, Site URL, redirects and SMTP settings are saved. A verified disposable owner completed explicitly approved signup and Terms acceptance on the domain. Confirmation arrived in the inbox at 13:24:51 UTC and recovery at 13:38:09 UTC on 7 September; both passed SPF, DKIM and DMARC. Confirmation, sign-in, session/workspace persistence on reload, sign-out and protected back navigation passed. Recovery updated the password; the old password was rejected once, the new password succeeded and the consumed recovery link was rejected. Session cookies were Secure, HttpOnly and SameSite=Lax. This does not yet prove refresh after actual token expiry or two unrelated identities. Evidence: `/tmp/maintaincode-live-auth-evidence.json`.
- Main CI and CodeQL passed at the audited `e86c4fddcb32eb075e9c23b461e630a543b5b38e` revision: 1,252 application tests in 144 files, lint, TypeScript, contract/configuration checks, 36-page production build, 26 migrations/85 database tests, Docker concurrency and persistent browser journeys, container packaging and authenticated readiness. Dedicated suites repeat tests from the main suite; totals are not added together. Runs: https://github.com/rory-hayes/maintainflow/actions/runs/34095928243 and https://github.com/rory-hayes/maintainflow/actions/runs/34095927767.
- That workspace-access correction was deployed as `dpl_6NpThvjnMRFYWMRh9uCv2KiCX3JK` in Dublin. Public-domain runtime proof at 07:11:58 UTC on 7 September verified the exact `80eb530` revision, runtime role, all six table grants/isolation policies, queue, recipient validator and www redirect without a Vercel bypass: `/tmp/maintaincode-public-domain-80eb530-probe-result.json`. Code verification and deployed/customer acceptance are distinct.
- The corrected revision passed all 37 live desktop, 320px/390px mobile, keyboard, sample-export and public auth/legal checks with no JavaScript exceptions. Signed-out pages no longer invent owner/customer access or show client-creation controls; a failed selection preserves the user's other verified workspace choices. Ten focused component tests cover signed-out, loading, failed access and recovery, actual membership and first-workspace creation. Live browser evidence: `/tmp/maintaincode-live-public-audit-1788765150205`. No registration, payment or provider action is included in this count.
- A fresh production dependency audit at `80eb530` reported zero known vulnerabilities: `/tmp/maintaincode-production-dependency-audit-80eb530.json`.
- Vercel's authenticated cron listing confirms maintenance is registered at `15 2 * * *`. Actual scheduled execution remains unobserved. A later owner-scoped manual run completed as recorded below; it is not scheduler evidence, and manual runs advance the workspace's next eligibility. No global maintenance call was made. Registration evidence: `/tmp/maintaincode-hosted-maintenance-readonly-evidence.json`.
- A local synthetic recovery drill restored 24 public tables, tenant data, role/RLS behavior and four encrypted credentials into a second temporary PostgreSQL 17 cluster. Its 463 implementation/migration/configuration files matched release `da088249` byte-for-byte. Both clusters and dumps were removed. This is local recovery-procedure evidence, not a production backup or hosted Auth-continuity guarantee.
- The verified disposable owner created and reloaded one normal trial workspace and one authorized HTTPS loopback fixture site. Its actual production tracker and collector passed required-consent gating, an independent successful HTML form callback, a browser-local 503 with navigation/retry recovery, duplicate identity handling and withdrawal. The loopback certificate exception was confined to the controlled visitor browser. This is controlled traffic through the real deployment, not genuine paid advertising, a public customer installation or provider delivery. Evidence: `/tmp/maintaincode-live-visitor-evidence/evidence.json`.
- The authenticated UI and downloaded workspace/CSV exports reconciled one production enquiry plus one diagnostic, usage `1 / 500`, one site and no connectors or CRM verification. The tagged ChatGPT channel showed one enquiry, zero qualified leads/won deals/booked value and Unknown spend. Sensitive export keys were absent; non-empty click-reference masking was not exercised. Evidence: `/tmp/maintaincode-live-export-evidence.json`.
- A naturally expired third diagnostic was physically removed by one owner-scoped manual maintenance request: stored rows went from three to two, both original control rows were unchanged, and the queue finished `complete` with no lease or failures. No direct SQL data writes or global maintenance requests were used. This proves deployed retention and manual queue completion, not the scheduled cron or provider refresh. Evidence: `/tmp/maintaincode-owned-retention-evidence.json`.

## Remaining customer acceptance

1. Complete Stripe authenticator sign-in, configure the four new prices and webhook, then verify test checkout, entitlement, cancellation and portal behavior. No real charges or new Stripe resources have been created. Missing new prices reject before Stripe calls, with no fallback to legacy prices. Agency selection was not exercised: automatic approval review rejected the potentially billable selection, and acceptance used a normal trial.
2. Connect authorized HubSpot and OpenAI Ads accounts. Verify actual form-editor compatibility, mapped first/latest CRM field delivery, outcomes and native Ads costs. Controlled tags, sample data and mocks are not provider acceptance.
3. Workspace report acceptance is **in progress**. Health-only opt-in, verified-recipient binding and installation-condition preflight passed. No report was sent: automatic approval review rejected sending pending explicit approval of the payload and recipient. Provider acceptance, inbox receipt and received-link opt-out remain pending. UI opt-out was saved and read-only database verification confirmed zero subscriptions or pending reports; both preferences are off. Authentication email delivery does not prove report delivery. The actual first weekly delivery after seven days remains a separate gate.
4. Observe an actual scheduled cron run, eligible-workspace processing and connector refresh. Manual maintenance and retention have passed; manual runs advance queue eligibility and cannot be counted as scheduled execution.
5. Verify session refresh after actual token expiry, permissions across two unrelated authenticated users, and live scoped site deletion. Pausing the test site stopped its tracker while the independent form still submitted with zero new captures. The test site was subsequently resumed; the temporary visitor browser, server and certificate were removed. One owner-controlled workspace and database isolation tests do not prove these journeys.
6. Establish a production backup destination/schedule and recovery objectives, then verify hosted restore/Auth continuity. The Free Supabase project has no scheduled managed backups or PITR. The successful local synthetic drill does not supply that protection. An external customer's first source-to-CRM value also remains unobserved.

## Handling and rollback

Prepared credentials remain in private local setup files and must not be committed. The old Supabase project and unrelated resources were preserved. Use only the dedicated runtime connection in the application. The old production deployment is `dpl_96FouefoPzCnGYtNHdR5DPg2EERN`; its source revision remains in Git history.

Authentication inbox delivery is proved for the verified disposable owner. Do not extend that claim to workspace reports, payments, real provider handoff or external-customer acceptance without their corresponding evidence.
