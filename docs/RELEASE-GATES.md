# Remaining implementation and release gates

## Current gates — 13 September 2026

The latest parser-copy checkpoint passed **11 focused cases** and **394/394 tests** before a final CSS-only mobile layout change. The final 196-file source passed **12/12 controlled desktop/mobile browser groups** and production build/package verification, including six decoders, API startup and the preview invitation guard. [Parser-copy acceptance](PARSER-COPY-ACCEPTANCE.md) preserves the suite/CSS boundary and earlier browser failures. Exact-head CI/CodeQL and Git publication still require their own evidence; no hosted copy deployment is claimed. Copy needs no new migration; hosted **021–026 remain unapplied**.

The earlier template-selection source passed **383/383 tests**, ten controlled desktop/mobile browser checks, two-family matching/nonmatch acceptance and build/package verification. Migration026 is local only. [Template acceptance](TEMPLATE-SELECTION-ACCEPTANCE.md).

The canonical invite-only preview remains on **`676a61a8b757931a649fb6b31b64d2f11937ce51`**, the released sign-in fix. Its eight hosted browser checks passed; the scoped authentication limiter is per runtime instance. Existing free hosting plans and visibly mocked billing remain unchanged. Google consent, approved-value delivery, replay and natural-expiry token refresh passed on the earlier `e446f6b` revision and were not rerun on the newer runtime. Actual Folio Resend intake remains unverified. [Current hosted status](HOSTED-PREVIEW-STATUS-2026-09-13.md) links the dated evidence and exact remaining approvals.

The earlier automatic-setup source passed **355/355 serial tests**, zero failures/skips/cancellations in **82.640 seconds**. Thirteen focused setup cases passed in **5.301 seconds**; `build:vercel` and the isolated runtime verifier passed six decoders, API startup and the preview invitation guard. One real automatic discovery/extraction matched the full synthetic invoice. That run stopped after explicit approval at the export selector; a separate **10-check replay** completed PDF rendering, approval/JSON export/reload and recovery without additional live calls. Hosted **021–025 remain unapplied**.

The earlier explicit field-suggestion checkpoint passed **342/342 tests** in **77.221 seconds**, with one real suggestion and a separate 12-check follow-up using its recorded output plus one real extraction; the later 14-check UI replay made no new calls. The pre-W03 C04 checkpoint passed **310 tests** in **71.212 seconds** and **24 browser checks**. Those dated results remain separate from automatic setup. The [47-row capability matrix](PARITY-MATRIX.md) retains every original criterion.

| Area | Current state | Remaining work and closure evidence |
| --- | --- | --- |
| API-key expiry and shared request limits | Migration **020 is applied**. PR 23 head `4965f09` passed six GitHub checks after 256 local tests. Migration **021 is prepared but unapplied**; the canonical runtime has neither feature. | Obtain the specific hosted migration approval that automatic approval review required for 021's table/privilege changes, apply and verify it, release a passing matching source, then verify natural key expiry, omitted-expiry compatibility and shared limits on the canonical runtime. Existing-key preservation is local-test evidence; the hosted pre-migration key count was zero. |
| C04 formats and durable source rejections | Migrations **022 and 023 are local only**; the completed 310-test/24-browser checkpoint proves the documented local scope. The provider-status browser row was explicitly synthetic; controlled tests separately prove worker completion/retry. | Apply/verify the migrations through the authorized hosted workflow, deploy matching code and verify actual hosted intake/replay/rejection. [Processing contract](ARCHITECTURE.md#processing-contract). |
| Earlier W03 explicit suggestions | Migration **024 is local only**. Existing same-parser uploads can be explicitly submitted for editable field/table drafts, with 60-field/four-level limits, request replay, quotas, shared concurrency, lease fencing and explicit schema save. Fourteen controlled adapter and eight hosted-worker tests pass. One real suggestion and a separate 12-check browser follow-up using that recorded suggestion plus one real extraction are evidenced. | The final local suite passed **342/342 tests**. The actual extraction matched the synthetic manifest, and 14 final UI replay checks plus hosted packaging/runtime verification passed; [W03 acceptance](SCHEMA-SUGGESTIONS-ACCEPTANCE.md) records the boundaries. Preserve the first stopped browser assertion and separate follow-up in the evidence; no single uninterrupted real-provider flow is claimed. Apply/verify 024 with matching runtime before hosted acceptance. That checkpoint did not include automatic first-upload setup. [Field suggestion contract](ARCHITECTURE.md#field-suggestion-contract). |
| W03 automatic first-document setup | Migration **025 is local only**. Explicit opt-in creates a persisted awaiting-sample parser; first intake queues one discovery request, saves the initial schema and releases existing held jobs. The final **355-test** suite and **13 setup cases** cover recovery, races and unchanged upload charges; packaging/runtime checks pass. | Preserve the real automatic discovery/extraction and separate 10-check browser replay, including the stopped live export-selector assertion and controlled recovery cases. Apply/verify 025 after 021–024 through the authorized hosted workflow. Deploy matching runtime and verify setup, recovery and metering. [First-document acceptance](AI-ASSISTED-SETUP-ACCEPTANCE.md). No automatic approval/export is implemented. |
| X06 complete text templates before AI | Migration **026 is local only**. Complete raw-source matching, most-field ranking, deterministic ties and pinned run history pass 28 focused cases, the final 383-test suite and ten browser checks. Native-text template calls bypass AI; one browser fallback uses a controlled provider only. | Apply/verify026 after021–025 and deploy matching runtime before hosted acceptance. Preserve old-job compatibility and current-saved preview versus pinned-history semantics. OCR regions and managed template libraries remain outside this native-text subset. [Acceptance](TEMPLATE-SELECTION-ACCEPTANCE.md). |
| W05 parser copy | Local acceptance passed: **11 focused cases**, **394 aggregate tests** before the CSS-only mobile fix, **12 final desktop/mobile browser groups**, and final build/package checks. Copy preserves saved configuration with fresh identities and copies no documents/history/jobs/usage/connections. | Obtain exact-head CI/CodeQL and Git/PR publication evidence separately. No new migration is required; deploy a passing matching source only after hosted 021–026 prerequisites are resolved. Retain non-idempotent submission semantics, active-list lost-response recovery, shared parser capacity and separate connection setup. [Parser-copy acceptance](PARSER-COPY-ACCEPTANCE.md). |
| Resend receiving | Account key/read access, dedicated webhook and a synthetic own-inbox probe passed. The prepared application settings have **not** been transferred to Vercel. | Two exact actions remain blocked by automatic approval review: transfer the eight prepared server settings and delete the unused superseded key. After authorized configuration and deployment, verify signed body-plus-PDF intake, private originals, extraction/jobs/usage and replay without duplication. Provider-account/probe success alone is not Folio intake proof. |
| Google Sheets | Consent, approved values, native readback, replay without a duplicate row and natural-expiry refresh passed on `e446f6b`. | Retest against the released runtime when required; refresh after restart and disconnect/revocation remain unverified. Google OAuth Testing still imposes its seven-day refresh-token lifetime. |
| Billing, production operation and remaining reference scope | Billing stays mocked on the existing free plans. Earlier local and hosted checks remain bounded to their identified revisions and synthetic fixtures. | Real Stripe testing/plan upgrades remain deferred. Managed verification/recovery/mail, backup/restore and broader deployment isolation/operating evidence remain separate gates. Managed template/OCR-region behavior and other unfinished matrix criteria remain open; parser copy and automatic first-upload setup have their own acceptance/release gates above. No customer-use or general extraction-accuracy claim follows. |

## Historical ledger — 8–9 September 2026

The text below preserves the earlier evidence and the blockers as they stood then. Its references to “current” source, pending approvals, undeployed infrastructure and unconfigured Google/Resend describe that dated checkpoint; the 13 September gates above supersede them. Historical commands and approval statements are not new instructions or current authorization requirements.

8 September 2026. Folio is a real local platform with deterministic processing and a configured native OpenAI extraction path. **Provider setup remains incomplete: Resend receiving and Google Sheets still require the precise account actions below. The user requested local mock billing; real Stripe testing is deferred. Full Parseur parity has not been reached.** Earlier Mac-unlock and AI browser-approval gates are resolved. The original BUILD-BRIEF is preserved; this ledger records the subsequent user instruction and current evidence.

Secure OpenAI setup and positive browser AI acceptance are complete. After explicit user approval, the browser created **AI receipt QA**, uploaded the synthetic PNG, reviewed the real worker result, approved revision zero, downloaded matching JSON and reloaded all seven lifecycle events. [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md) records identities, actual bytes, model/prompt/tokens, qualified evidence and usage. Earlier automatic approval rejection and locked-Mac checkpoints remain historical. Real receiving and Sheets verification still require their own accounts/configuration; real Stripe testing is deferred by the user. No production-readiness claim follows.

The current source passed **184/184 tests**, zero failures/skips, from `2026-09-08T20:27:19.867548Z` to `20:27:50.185332Z`; TypeScript/Vite build passed from `20:27:50.186090Z` to `20:27:52.235746Z`. [Verification](evidence/provider-setup-2026-09-08/verification.json) links the actual outputs/timings and identifies **191 files**, fingerprint `a1b67de414f66a5724ead433ff286f28620a77014a8f41f0f331cebef9de99e0`, unchanged during verification. Seventeen added cases cover the local billing mock and managed receiving support. Migrations remain 001–008. The [167-test workspace checkpoint](WORKSPACE-INTEGRITY.md), preceding 160-test/browser evidence, independent **49/51** held-out result and **51/51** regression replay remain historical, unaltered evidence. [Provider setup](PROVIDER-SETUP.md) records the current configuration, prepared artifacts and pending approvals; the [mock billing browser workflow](evidence/provider-setup-2026-09-08/billing-browser.json) passed selection/change/cancel/reload, with real provider gates still open.

### Required acceptance and remaining local work

9 September refresh: [current prerequisite evidence](evidence/provider-setup-2026-09-09/prerequisites.json) confirms the 191 verified source files are unchanged. The stopped local services were restarted and all three health checks return HTTP 200; [browser persistence](evidence/provider-setup-2026-09-09/browser-after-restart.json) retains the Team mock allowance and eight documents. Mac access is restored, but native Sheet import remains incomplete and the direct Chrome upload still requires extension file-URL access. The three specific provider/account approvals remain unanswered. No external success or new test-suite run is inferred from this refresh.

| Priority / matrix IDs | Current gap | Concrete work and closure evidence |
| --- | --- | --- |
| P0 — X01, X03 | Native AI/vision implementation, regression acceptance and positive browser workflow passed | `openai-provider.ts` uses pinned `gpt-5.4-mini-2026-03-17`, strict recursive schema validation, raw-preserving normalization, page-text checks and explicitly qualified visual evidence, limits/cancellation/retries and stored model/token/cost provenance. The aggregate includes 18 provider, 9 worker, 9 CSV and 2 written-date tests. The independent first pass measured 49/51, and the unchanged-input replay after the date-selection correction measured 51/51. Both include an exact duplicate image. Missing required fields/evidence warnings remain review issues; a prior v1 freeform call took 57.011 seconds. The [actual AI receipt browser workflow](AI-BROWSER-ACCEPTANCE.md) completed parser creation → original upload → worker result → image/evidence review → uncorrected approval → JSON download → reload. Four approved values match the receipt, with raw total and visual warnings preserved. [AI-EXTRACTION.md](AI-EXTRACTION.md) records the exact contract and limits. |
| P0 — W04, R03, O06, O08 | Central deterministic, AI receipt and source-disclosure browser acceptance passed | [BROWSER-QA.md](BROWSER-QA.md) records desktop/tablet/mobile routes, actual sample and two-page PDF intake, review/correction/approval, real downloads, two-account isolation, focus/navigation and screenshots. A 26-document three-page pagination/deletion check, member role/removal/invitation revocation, template deletion and synthetic webhook management controls also passed. German receipt approval/export and HTML table review retain direct evidence. The immutable table/object source disclosures pass controlled rendering. [Browser verification](REVIEW-PROVENANCE.md) confirms table-level quotes, original rows, keyboard operation, source-page navigation and mobile Document switching; a targeted table-wrapper CSS fix removed the observed 422px overflow at a 390px viewport. Nested/deep traversal and structural row edits remain controlled-test evidence. Explicit approval and Mac unlock enabled the completed [AI receipt workflow](AI-BROWSER-ACCEPTANCE.md). The design review retains visual differences; large-load behavior and arbitrary document layouts remain outside this pass. |
| P0 — C05, C06, E01 | Actual processing and export lifecycle acceptance passed | Migration 008 journals received → queued → processing → needs_review → processed/approval and failure. Seven lifecycle tests verify operation identity, persistence, tenant isolation, immutable events, stable newest-200 ordering and deletion cascade. Six export tests verify exporting precedes actual rendering and exported follows persistence under one operation UUID. Render/SQL failures commit safe failed phases while rolling back partial snapshots/status changes; invalid, unauthorized and unapproved requests create no export phases. Historical approved export preserves newer needs_review. [Browser history](evidence/lifecycle-browser.json) and [screenshot](evidence/lifecycle-approved-export.png) show all seven successful events for the German receipt. Synchronous export phases are durable history, not a new background export queue; historical documents are not backfilled with invented events. |
| P0/P1 — E01–E04, E07 | Local export, bearer API, webhook and recipe acceptance passed; real destination proof remains external | Integration/automation tests cover actual CSV/XLSX/JSON bytes, approved snapshot selection, nested mapping, formula-safe cells, tenant checks, deletion, signatures/retry/replay and example payload contracts. [Browser QA](BROWSER-QA.md) verifies actual downloads and persisted custom mapping; the [German receipt download](evidence/approved-locale-receipt.json) preserves numeric total 1234.56 and date 2026-09-17. Five bearer API cases verify multipart upload, exact job processing/results, dedupe, scopes, tenant denial and revocation. No real automation-provider account or published native connector is claimed. |
| P0/P1 — W01, W05, C02, C04, C08 | Required local source-format and intake-policy acceptance passed | Ten source-format cases verify JPEG/image-only PDF metadata and unchanged private original downloads after explicit provider-needed failure, no fabricated OCR result, representative HTML entities/list/multiline/long-value/flat-table fidelity, HTML-only EML conversion, plain-text MIME precedence, and incomplete DOCX/XLSX rejection. [Actual HTML browser values](evidence/html-browser-values.json) and [review screenshot](evidence/review-html-table.png) preserve Cedar & Pine and numeric 18.6 while the absent Date remains a review issue. Three intake-policy cases prove archived-parser rejection without files/jobs/usage, signed/retrieved disallowed-sender rejection before intake with a safe audit reason and positive control, and expired-session denial while a fresh session works. General complex layouts are not inferred from those source-reader fixtures; configured vision regression results are recorded separately above. |
| P0 — W05, W07, O01, O05 | Active parser restoration and membership audit integrity passed | Parser create/archive/restore share a workspace transaction lock; restoring into a full active allowance returns 429 without changes/audit, ordinary edits remain usable, and competing restores/creation claim only one remaining slot. Invitation acceptance/revocation/deletion, role changes and removal record minimal actor/entity/role events in their mutation transaction; no-op/rejected requests emit no success event. Two parser and five membership cases pass, including audit-failure rollback of membership/key/session effects. [WORKSPACE-INTEGRITY.md](WORKSPACE-INTEGRITY.md) records exact guarantees and limits. |
| P0 — O05 | Local defensive review, bounded decoder and interrupted-intake recovery passed; deployment isolation remains open | The final aggregate and nine decoder/recovery regressions cover tested auth/RLS/roles/scopes, immutable outputs/deletion, subprocess input/output/deadline/concurrency controls, and durable orphan/reservation recovery with forward progress. [DECODER-RECOVERY.md](DECODER-RECOVERY.md) records exact limits. The subprocess still has the OS user’s filesystem/network privileges; OS/container identity, egress, CPU/full-memory/process quotas and safe deployment resource tests remain gates. No certification or exhaustive vulnerability-free claim. |
| P1 — W06, X02, R01, R02, R05 | Schema, normalization and safe transform acceptance passed; broader extraction quality remains bounded | [Browser QA](BROWSER-QA.md#final-schema-refinement) records a saved Department default, Operations/Finance enum correction, and nested line_items.item_meta.cost_centre creation/reload/removal as immutable schema versions. Required-value approval blocking also passed. Twelve extraction cases verify multiline boundaries, defaults, raw-preserving transforms and total-mismatch issues; a scoped approval case proves rejection then corrected approval. Real browser multiline and German-locale documents passed approval/JSON export. Date fields retain the written calendar date; timestamp/timezone conversion is unsupported. Arbitrary nested/freeform quality remains unproven; arbitrary Python execution remains outside initial scope. |
| P1 — O03, O04 | Configured age expiry, processing notifications and export failure history passed | [NOTIFICATIONS-RETENTION.md](NOTIFICATIONS-RETENTION.md) records four passing local regressions: aged originals/derived copies/read receipts removed, active/fresh/other-workspace documents preserved, per-user read isolation and exact document safety, and preference hide/restore. [Browser QA](BROWSER-QA.md#processing-notifications) proves persistence, mobile dialog/focus and real outcome links. Export failures have separate durable document-history entries with safe fixed reasons; their six-case suite verifies rollback and retry. Unattended multi-day/load soak and production backup erasure remain operational evidence; no notification emails are implemented. |
| P1 — O08 | Current tests/build/source identity and mock browser acceptance pass; real provider outcomes remain separate | The [current verification](evidence/provider-setup-2026-09-08/verification.json) links 184 passing tests, a successful build and unchanged 191-file source identity. Historical 167/160-test, AI browser and held-out/regression evidence remains intact. [Provider setup](PROVIDER-SETUP.md) records the requested local billing mock, managed receiving checks and current external blockers. The [mock browser workflow](evidence/provider-setup-2026-09-08/billing-browser.json) passed; no real Resend/Sheets delivery or Stripe transaction is inferred. |

### External prerequisites and exact unblocks

| Capability | Independent adapter state | External unblock | Evidence needed after unblock |
| --- | --- | --- | --- |
| OpenAI extraction | **Configured and verified within the synthetic regression and receipt-browser scope**; strict adapter and controlled worker paths pass; real native/freeform/PNG/JPEG/image-only PDF requests succeeded | Credential, Mac-unlock and explicit browser-approval prerequisites resolved | [Actual browser result](AI-BROWSER-ACCEPTANCE.md) records source comparison, uncorrected approval, JSON bytes, reload, seven lifecycle events and usage. Retain nulls, evidence warnings, latency and earlier failed evaluations; neither the regression suite nor one successful browser receipt establishes general accuracy |
| Resend inbound | Custom-domain MX and managed-inbox probe paths implemented; controlled routing/replay and eight additional managed cases pass | Existing account and `deubdel.resend.app` observed. Full access/all-domains key creation and synthetic probe sending need explicit approval; key not created, probe not sent. Then record the received-email probe ID, configure signed public HTTPS webhook and enable inbound | Probe retrieval alone is not delivery proof. Verify a separate body/attachment email through signed webhook → exact parser → files/jobs/audit/usage, plus replay and rejected-routing behavior. Preserve custom MX support; see [PROVIDER-SETUP.md](PROVIDER-SETUP.md) |
| Google Sheets | Workspace OAuth and deterministic RAW range adapter implemented; controlled fixtures pass; local workbook prepared | Native Sheet import is blocked by connector file-schema validation and Chrome file-URL permission; a later native picker fallback is blocked by a new 8 September Mac lock. Only a private empty folder exists. Cloud project creation was rejected and its exact approval is pending, followed by OAuth client/API/callback configuration and consent | Actual native worksheet, headers/rows, no duplicate retry rows, refreshed token after restart, denied permissions and disconnect/revocation. A workbook/folder or general Sheet consent does not establish Folio OAuth or delivery |
| Billing — requested local mock | Explicit opt-in mock persists local plans/limits and audit; nine backend/UI tests pass; production disables it and Stripe transports are blocked | No Stripe account prerequisite for the current user-requested mock. Browser Standard → Team → cancel to Explore → restore Team → reload passed | The [browser/DB proof](evidence/provider-setup-2026-09-08/runtime-and-billing-state.json) confirms labelled Team mock limits, eight preserved documents, four matching audit events and no Stripe subscription/Checkout rows. Real Stripe Checkout/portal/webhook proof is deferred by the user and remains distinct future work; no payments or actual subscriptions are claimed |
| Production authentication/mail | Local auth/manual invitation path implemented; production flows still need code | Selected managed service or verified transactional sender/domain, provider account settings and deployment URLs | Real verification, recovery and invitation email delivery; wrong/expired/replayed links rejected; session and role changes confirmed |
| Deployment/private storage | Local PostgreSQL/private-file runtime works; no cloud deployment configured | User-authorized hosting/runtime/database/storage choice, deployment access, secrets, TLS/domain and persistent-volume/object-storage arrangements | Identified deployed revision, migrations/least-privilege roles, same-origin routing, private-object access/deletion, supervised worker recovery, backup/restore, health/alerting checks |

Provider configuration details and constraints are in [PROVIDERS.md](PROVIDERS.md) and [AI-EXTRACTION.md](AI-EXTRACTION.md). Secure OpenAI setup and actual synthetic-document requests are now evidenced. No real Resend inbox receipt, Google account delivery or Stripe Checkout/portal flow has been verified. Deployment, paid service/domain purchases, production launch and customer contact are not implied by local startup or this gate list.

### Extended reference scope retained as gaps

These are not blockers on local testing of implemented behavior, and they must not be advertised as shipped parity:

- Additional file converters beyond the current PDF/images/text/EML/CSV/XLSX/DOCX/HTML readers. Representative HTML and HTML-only EML fidelity, flat tables and incomplete Office rejection now have passing fixtures; broad complex-layout fidelity is not claimed.
- PDF splitting, archive-to-multiple-document intake, deskew/repair, zonal/dynamic coordinate extraction, broad OCR language coverage, and provider-calibrated confidence.
- AI-suggested schemas from the first upload, full AI/template engine precedence, richer file-type mailbox policies, parser copy, and structured address/geolocation providers.
- Optional API-key expiry is not implemented; current keys are scoped, hashed, shown once and revocable. Session expiry is implemented and verified separately. Timestamp/timezone conversion is unsupported; saved locale settings normalize documented calendar-date and number formats only.
- Reference-native marketplace connectors; additional Drive/Dropbox/OneDrive/SharePoint intake recipes beyond the base authenticated upload bridge.
- Arbitrary tenant-supplied Python execution, enterprise certifications/contracts, public uptime promises, unlimited scale, and production payment activation.

The required AI adapter and the real synthetic-receipt browser workflow are verified within their recorded scope. The other provider accounts retain their exact external prerequisites. Extended scope needs an explicit future implementation decision; it cannot be silently converted into a working claim.

### Completion order

1. Preserve the completed browser/lifecycle/export/recipe/schema/source-format/intake-policy/retention and bounded-decoder/recovery evidence, historical 107/135-test records and current 160-test source record and retained live-AI evidence; rerun relevant checks when source changes.
2. Preserve the completed AI receipt creation/upload/review/approval/JSON/reload and source-disclosure/mobile/keyboard evidence. The viewport was reset and its 160-test/build source checkpoint passed; the subsequent 167-test workspace-integrity checkpoint is retained; current provider source passes 184 tests and the build. No further credential setup or browser approval is required for this configured OpenAI workflow.
3. Preserve the completed local mock billing browser acceptance. Finish Resend receiving and Sheets setup after the exact permissions/settings are available, then verify their actual delivery paths. Real Stripe testing is deferred by the user.
4. Update all 47 parity rows and the verification ledger with current evidence. Keep genuine external blockers precise. Do not mark the overall goal complete while required work remains.

Production authentication/recovery, deployment and actual customer use are distinct future evidence layers. Local sign-up/sign-in/logout, password change, manual invitations and roles are implemented; managed verification/recovery/email delivery remain production gates. Neither production deployment nor customer use is a requirement to run the local application, and neither has been inferred from code, tests, screenshots, or public-reference claims.

Historical local handoff checkpoint: [v2 runtime](evidence/openai-date-local-runtime.json) confirms HTTP 200 web/API/presets and observed worker startup after tests; [redacted credential check](evidence/openai-date-key-boundary-check.json) confirms mode 600, Git ignored/untracked and no key match across 172 client files. AI browser approval was still pending at that checkpoint; the later [completed workflow](AI-BROWSER-ACCEPTANCE.md) resolves it.

The [redacted runtime/configuration checkpoint](evidence/review-provenance-runtime.json), `2026-09-07T09:22:12.443Z`, records web/API/presets HTTP 200, OpenAI configured and worker startup. Required Resend, Google OAuth and Stripe test environment settings were absent. The Mac was locked at that checkpoint; the user subsequently unlocked it and explicitly approved the completed AI workflow.

The historical [redacted runtime/configuration check](evidence/ai-browser-runtime.json), recorded `2026-09-07T17:19:42.405Z`, confirms web/API health/presets HTTP 200, observed worker startup and OpenAI configuration. At that checkpoint Stripe test, Resend and Google OAuth settings were absent, with Resend inbound disabled. No credential values were printed.


After the 7 September 167-test/build run, the local services restarted and a read-only browser reload confirmed the existing AI receipt remained **Run 1 · Approved / Exported**. Document/body widths were 1280px at the default 1280×720 viewport; captured browser warnings/errors were empty. [Browser checkpoint](evidence/workspace-integrity-browser.json), [DOM](evidence/workspace-integrity-browser.txt), [screenshot](evidence/workspace-integrity-browser.png). This is persistence/smoke evidence, not a new browser membership or parser-capacity mutation test. The [historical redacted runtime check](evidence/workspace-integrity-runtime.json), `2026-09-07T17:33:01.314Z`, confirms web/API health/presets HTTP 200 and observed API/worker/Vite startup. At that checkpoint OpenAI was configured; Stripe test, Resend and Google OAuth settings were absent, with Resend inbound disabled.

[Historical prerequisite check](evidence/provider-prerequisites-check.json), 7 September 2026 at 17:35 UTC: all 186 then-verified source files were unchanged, local development was running, and Resend, Google OAuth and Stripe test settings were absent. The native goal was blocked at that checkpoint. On 8 September the user supplied Resend account access, consented to creating a Sheet and requested mock billing. [The current setup ledger](PROVIDER-SETUP.md) records the new outcomes and exact remaining permissions without treating the earlier checkpoint as current.

Current [runtime and billing-state readback](evidence/provider-setup-2026-09-08/runtime-and-billing-state.json) confirms HTTP 200 for web, API health and the registered `/api/presets` route at `2026-09-08T20:33:22.739Z`. The initial `/api/public/presets` diagnostic returned 404 because that route does not exist; its correction is recorded. The [browser result](evidence/provider-setup-2026-09-08/billing-browser.json) confirms persisted Team mock limits, desktop/mobile layout without root overflow, empty captured warning/error lists and viewport reset. Email intake address creation and Google Sheets connection remain disabled with Setup required. No outstanding account approval has been received.
