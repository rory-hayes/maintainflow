# Provider setup and requested billing mock — 8 September 2026

The user supplied access to the existing Resend account, consented to creating a Google Sheet, and requested **mock Stripe for now**. The original [BUILD-BRIEF](../BUILD-BRIEF.md) is unchanged; this dated ledger records that subsequent scope instruction. Real Stripe testing is deferred. Resend receiving and Google Sheets delivery remain incomplete.

## Current source verification

The complete suite passed **184/184 tests**, zero failures/skips, from `2026-09-08T20:27:19.867548Z` to `20:27:50.185332Z`. TypeScript/Vite build passed from `20:27:50.186090Z` to `20:27:52.235746Z`. [Test output](evidence/provider-setup-2026-09-08/tests-results.txt), [test timing](evidence/provider-setup-2026-09-08/tests-run.json), [build output](evidence/provider-setup-2026-09-08/build-results.txt) and [build timing](evidence/provider-setup-2026-09-08/build-run.json) preserve the actual commands and exits.

[Verification](evidence/provider-setup-2026-09-08/verification.json) and [source manifest](evidence/provider-setup-2026-09-08/source-before.json) identify **191 files**, SHA-256 `a1b67de414f66a5724ead433ff286f28620a77014a8f41f0f331cebef9de99e0`, unchanged during verification. The 17 new cases comprise six mock billing backend tests, three mock UI tests, seven managed receiving tests and one managed route case in the now ten-test provider suite. These controlled tests make no external provider requests. Historical [167-test workspace evidence](WORKSPACE-INTEGRITY.md), 160-test/browser proof and AI evaluations retain their original identities and dates.

## Billing mock

`FOLIO_BILLING_MOCK=true` is configured locally. Explicit opt-in is required, and production disables the feature. Owner/admin browser sessions can select Standard or Team mock limits or cancel to Explore. Changes persist in the actual local workspace plan with atomic audit records; repeating a state is inert. Downgrades preserve existing documents/parsers. Existing Stripe billing state prevents mock changes, and mock mode blocks Stripe Checkout, portal, webhook processing and reconciliation before transport.

The UI identifies **Local mock billing** and **Mock mode · no payments**; provider status retains `configured: false` and `verified: false`. Nine backend/rendering tests pass. The [actual browser workflow](evidence/provider-setup-2026-09-08/billing-browser.json) passed Standard → Team → cancel to Explore → restore Team → reload. Team (local mock) persisted with **39/5,000 pages used**, **50 parser capacity** and **four concurrent jobs**. All **eight documents** remained. [DB readback](evidence/provider-setup-2026-09-08/runtime-and-billing-state.json) records the four matching mock audit events and zero subscription/Checkout rows. No real payment, Stripe customer, Checkout or subscription was created for this setup.

[Desktop evidence](evidence/provider-setup-2026-09-08/billing-desktop-viewport.png) was captured at 1280×720 with body/document width 1280px. [Mobile top](evidence/provider-setup-2026-09-08/billing-mobile-top.png) and [mobile plans](evidence/provider-setup-2026-09-08/billing-mobile-plans.png) were inspected at 390×844 with body/document width 390px. Mobile verification covers layout only; mutations were desktop-only. The temporary viewport was reset and captured browser warnings/errors were empty. [Reload text](evidence/provider-setup-2026-09-08/billing-reload-usage.txt) retains the persisted allowance.

The [current runtime check](evidence/provider-setup-2026-09-08/runtime-and-billing-state.json) returned HTTP 200 for web, API health and `/api/presets` at `2026-09-08T20:33:22.739Z`; worker startup was observed. The first diagnostic used nonexistent `/api/public/presets` and returned 404; the record retains that incorrect probe and the corrected result. [Receiving gate browser text](evidence/provider-setup-2026-09-08/receiving-gate-browser.txt) shows Setup required with inbound-address creation disabled; [the browser record](evidence/provider-setup-2026-09-08/billing-browser.json) separately records Google Sheets connection disabled. These runtime checks do not establish external delivery. The [final handoff integrity record](evidence/provider-setup-2026-09-08/handoff-integrity.json) confirms all 191 source hashes unchanged after browser verification at `2026-09-08T20:35:39Z`; the local server environment remains mode 0600 and Git ignored.

## Resend receiving

[Account setup evidence](evidence/provider-setup-2026-09-08/account-setup-status.json) records access to the existing Resend account and its managed inbox `deubdel.resend.app`, with zero received messages observed. Existing custom-domain/DNS settings were left intact. Local configuration selects managed mode and that domain, but inbound remains disabled; a dedicated API key and signed webhook endpoint are not configured.

The key form is prepared as **Folio local inbound development**, requesting **Full access / all domains**. The key has not been created and its specific approval is pending. Any key must remain in the ignored server configuration; no key value belongs in this evidence.

The owned synthetic probe is prepared in [probe metadata](evidence/provider-setup-2026-09-08/managed-probe-prepared.json) and [plain-text email](evidence/provider-setup-2026-09-08/managed-probe-prepared.eml). It contains no customer data and has **not been sent**. Automatic approval review rejected opening Gmail because the supplied Resend/Sheets consent did not explicitly authorize Gmail access or sending the probe. Explicit permission for that specific access/send remains pending.

The managed contract in [`.env.example`](../.env.example) requires the exact single-label `<id>.resend.app` domain, a fresh `folio-probe-<32 lowercase random hex characters>` recipient, a 32–64-character lowercase hex nonce, and the UUID of the **received** email. Subject and plain-text body must both be `Folio receiving probe <nonce>`, without signatures or added content. Retrieved ID, exact sole `received_for` recipient, subject and trimmed plain-text body must all match. A successful probe proves retrieval only and reports `deliveryVerified: false`; a separate delivered message and publicly reachable HTTPS signed webhook are still needed to prove parser intake.

Custom-domain receiving remains supported through its existing exact domain-ID/name, enabled receiving and verified MX checks. In both modes, routing intersects signed and retrieved provider `received_for` values and ignores visible To/Cc. Resend derives `received_for` from Received headers; this does not independently prove SMTP-envelope or sender identity. [PROVIDERS.md](PROVIDERS.md) records all settings, retry limits and body/attachment behavior.

## Google destination and OAuth

[Destination evidence](evidence/provider-setup-2026-09-08/sheets-destination.json) records only a private, empty **ChatGPT** folder created in My Drive and a locally prepared **Folio — Receipt exports** workbook. The workbook’s Receipts and Setup tabs were rendered and inspected; receipt rows are blank and its note says automatic Folio OAuth sync is not connected. **No native Google Sheet exists**, and there is no spreadsheet ID or destination URL.

The connector import failed file-reference schema validation. Browser import also failed with `fileChooser.setFiles: Not allowed`; its documented fix requires enabling **Allow access to file URLs** for the ChatGPT Chrome extension. That permission is still pending. These attempts did not upload the workbook or establish a native Sheet. A final supported native file-picker fallback then reported that the Mac was locked and automatic unlock failed. That new 8 September lock blocks the native import fallback; the earlier AI workflow unlock and the completed billing browser evidence remain resolved historical facts.

Separately, automatic approval review rejected creating **Folio Sheets Development** in Google Cloud: permission to create a Sheet did not specifically authorize a persistent Cloud project with quota and potential billing implications. The project has not been created and its exact approval remains pending. OAuth client, Sheets API, callback and consent configuration are consequently incomplete. Creating a destination Sheet will not itself establish Folio’s OAuth connection, token refresh, row delivery or revocation.

## Remaining acceptance

1. Preserve the completed mock browser selection/change/cancel/reload and runtime/DB evidence; repeat relevant checks when its implementation changes.
2. Obtain the exact outstanding Resend key and Gmail probe permissions; complete managed probe retrieval, signed webhook registration and a separate actual body/attachment intake test.
3. Resolve native Sheet import and the separate Cloud project approval; configure OAuth, then verify real consent, cell delivery/retry, refresh and disconnect.
4. Keep actual Stripe testing deferred until the user requests it. Production deployment, managed authentication/storage and customer use remain separate, unverified evidence layers.

## Prerequisite refresh — 9 September 2026

The [current check](evidence/provider-setup-2026-09-09/prerequisites.json) confirms all 191 previously verified source files are unchanged. The previous development-process handle was missing and both application ports had no listener, so the existing private database, API, worker and Vite preview were restarted. Web, API health and `/api/presets` return HTTP 200; API and worker startup were observed. The [browser reload](evidence/provider-setup-2026-09-09/browser-after-restart.json) retained Team (local mock), 39/5,000 pages used, four concurrent jobs, 50 parser capacity and eight documents, with no captured console warnings/errors. This is new restart/persistence evidence; the 184-test/build result above remains the unchanged 8 September source checkpoint.

The Mac is accessible again. The native upload menu did not produce a usable file picker, and work on that surface stopped when the foreground changed to an unrelated tab. A direct upload in the dedicated Drive tab still returned `fileChooser.setFiles: Not allowed`; the documented extension file-URL permission remains necessary for that route. No workbook was uploaded and no native Sheet was created. The specific Resend key, Google Cloud project and Gmail probe approvals are still unanswered; none of those account actions was retried. Resend remains disabled and Google OAuth settings remain absent. The full goal remains active and incomplete.

At the subsequent 9 September check, the same provider approvals remained unanswered for the third consecutive goal turn. The 191 source hashes were unchanged; Resend API/webhook credentials and Google OAuth settings were still absent. Development session `35454` was confirmed running, with listeners on ports 5178 and 4318, so it was not restarted. The native goal was marked **blocked**, preserving its full objective, pending those approvals and the Sheet upload prerequisite. This supersedes the active status in the earlier refresh; it does not mark any external integration complete.
