# Hosted preview status — 13 September 2026

Folio is live at [maintainflow.io](https://maintainflow.io) as an invite-only preview on the existing free hosting plans, with billing visibly mocked. The current canonical revision is `676a61a8b757931a649fb6b31b64d2f11937ce51`, verified after the sign-in fix in [PR 25](https://github.com/rory-hayes/maintainflow/pull/25) deployed on 13 September. Eight hosted browser checks passed at 12:31:33 UTC. Google Sheets authorization, approved-value delivery, replay without a duplicate row, and natural-expiry refresh passed on the earlier `e446f6bd74c910c8fafa8b3973239c3e216f1735` revision; those provider workflows were not rerun on the sign-in release. Actual Folio Resend intake remains unverified.

The [12 September checkpoint](HOSTED-PREVIEW-STATUS-2026-09-12.md) retains the earlier deployment, browser, storage, tenant-isolation and scheduler-recovery evidence. Those dated results have not been rewritten as tests of newer work.

## Current evidence

| Area | Verified scope | Remaining boundary |
| --- | --- | --- |
| Sign-in | PR 25 is merged and live on `676a61a`. Its isolated source passed **238 tests**, **12 local browser checks**, hosted packaging and **6 decoder-bundle checks**. All **6 PR checks** passed on head `57ce0d1`; **8 canonical browser checks** then passed on the deployed merge revision. Short and long invalid passwords return the same generic 401; sign-up retains its password-creation minimum. | This release made no hosted schema or environment changes. Login and registration share a scoped **30 requests per 15 minutes** in-memory limiter per runtime instance; the shared PostgreSQL limiter in PR 23 is not live. |
| Google Sheets | On `e446f6b`: real consent; synthetic two-page invoice upload and rules extraction; correction and pinned approval; HTTP 200 delivery with native Sheet readback. Explicit replay preserved one data row with no duplicate. | Not rerun on `676a61a`. Provider disconnect/revocation and refresh after a runtime restart have not been tested. |
| Google token refresh | On `e446f6b`, the stored access token expired naturally. A new correction and approval triggered delivery, changed the access token, persisted a future expiry, and retained the refresh token. Native readback found the original row plus exactly one new row for revision 2. | That test kept the deployment unchanged; no restart or expiry-metadata manipulation occurred. It does not verify refresh on the newer sign-in runtime. |
| Resend | A Full access key, dedicated canonical webhook and synthetic probe to the account's own managed inbox were verified. The private application environment bundle is prepared. | The bundle has not been applied to Vercel; actual signed Folio body/attachment intake, extraction and metering have not passed. |
| API-key expiry | The earlier 13 September local expiry checkpoint on `codex/api-key-expiry`, based on `e446f6b`, passed **238 serial tests**, typecheck/build and **14 local desktop/mobile browser checks**. Post-fix expiry and hosted packaging/runtime checks also passed. The source reconciled with PR 25 subsequently passed **256 local tests** and typecheck; all **6 GitHub checks** now pass on PR 23 head `4965f09`. | Migration 020 is applied and verified. Hosted acceptance of the reconciled source remains pending; the earlier browser results do not establish that outcome. API-key expiry is not yet deployed. |
| Shared request limits | PR 23 implements global Fastify onRequest protection with atomic PostgreSQL counters, a stricter authentication budget, private IP digests, forced RLS and bounded cleanup. An earlier 13 September source checkpoint passed **247 tests**, packaging/typecheck and **14 browser checks**; the later **256-test** reconciliation and green PR checks are recorded above. | Migration 021 is prepared and reviewed but not applied. Chrome is unlocked; automatic approval review rejected running its hosted table and privilege changes, and the exact migration approval remains unanswered. Deployment and hosted acceptance remain pending. |
| C04 format policies and source rejections | The combined local source passed **310 tests**, zero failures/skips in **71.212 seconds**, final hosted bundle/build checks and **24 local desktop/mobile browser checks** on 13 September. Controlled tests prove typed permanent rejection receipts, replay before decoding, atomic audit/receipt rollback, mixed/all-rejected worker completion and continuation past transient items. Browser checks cover real rejection diagnostics and a clearly labelled synthetic provider-status row. | Migrations **022 and 023 are local only**. No C04 deployment, live Resend receipt or production verification is claimed. The provider-status UI fixture is separate from the controlled worker-finalization tests. |
| Billing and hosting | Existing free hosting plans remain in use. Billing remains mocked. | No real payment or customer-use acceptance is claimed. |

The sign-in [CI run](https://github.com/rory-hayes/maintainflow/actions/runs/34757148562) and [CodeQL analyses](https://github.com/rory-hayes/maintainflow/actions/runs/34757147678) passed on the exact PR head. The six checks comprise CI, both CodeQL language analyses, the aggregate CodeQL gate, Vercel preview and preview comments. The hosted checks verified the canonical revision, generic invalid-login responses, desktop/mobile layout, retained sign-up guidance and no account or session creation. They used three synthetic invalid-login requests. No page errors were observed; the immediate post-deployment runtime-error check found none, within that short observation window only. Private browser and deployment receipts remain in the ignored local handoff records.

PR 23's reconciled head `4965f09` separately passed all six checks, including [CI](https://github.com/rory-hayes/maintainflow/actions/runs/34757878056), [both CodeQL analyses](https://github.com/rory-hayes/maintainflow/actions/runs/34757875653), the aggregate CodeQL gate, Vercel preview and preview comments. A passing preview does not apply migration 021 or replace the canonical runtime.

The current local C04 browser receipt records 24 passing checks at 13:05:42 UTC, including format selection/reload, an empty-selection guard, preserved receipt Date, mixed upload outcomes, accepted-only usage, actual source-rejection reasons after reload, mobile layout and keyboard navigation to Activity. Its completed provider-event row is an explicitly synthetic rendering fixture; [controlled Resend tests](../tests/resend-format-policy.test.ts) separately exercise actual worker status transitions. No unexpected browser errors were recorded; intentional unauthenticated, format-policy and source-validation requests returned 401, 415 and 400 respectively. Local fixtures were removed. The [processing contract](ARCHITECTURE.md#processing-contract) describes the bounded classification and retry behavior; local receipts remain outside the repository.

The public-safe [Sheets delivery/replay receipt](evidence/provider-acceptance-2026-09-13/google-sheets-acceptance-public-safe-2026-09-13.json) records the approved values, numeric total and revision comparison, followed by a second complete 26,000-cell readback with no duplicate rows or formula cells. The [natural-refresh receipt](evidence/provider-acceptance-2026-09-13/google-natural-refresh-public-safe-2026-09-13.json) records the later token and row observations. Both remain evidence for `e446f6b`. Account identities, Sheet identifiers, application resource identifiers and credentials are omitted. These are synthetic acceptance checks, not evidence of general customer workflows.

The earlier PR 23 CodeQL gate identified 61 findings under one real missing-rate-limiting rule. Its shared control is implemented and locally verified after reconciliation, and the exact-head checks now pass; deployment remains pending. Findings have not been dismissed or the rule disabled. PR 25's separate scoped limiter passed its own exact-head checks. [Request-limit behavior and dated evidence](REQUEST-RATE-LIMITS.md).

## Next release steps

1. Migration 020 is applied and verified. Obtain the unanswered approval to apply prepared migration 021 in the existing hosted SQL Editor, then verify its results. Automatic approval review rejected the Run action because the hosted table and privilege changes require specific authorization; Mac unlock did not supply that approval. Release PR 23 only from its passing head, then verify the deployed expiry/shared request-limit runtime. The reconciliation preserves generic sign-in behavior while replacing the scoped in-memory limiter. See [API-key expiry](API-KEY-EXPIRY.md), including its rollback constraint.
2. Resolve the two pending exact Resend approvals: transferring the prepared eight settings, including server credentials, to Vercel production; and deleting the unused superseded key. Automatic approval review rejected those actions pending explicit authorization. Neither action has been performed.
3. After the authorized environment update and deployment, run the prepared synthetic body-plus-PDF receiving check through the actual provider webhook, private originals, extraction jobs and usage ledger. Repeat the observation to check for duplicate processing.
4. Before releasing C04, apply and verify migrations 022 and 023 through the authorized hosted migration workflow, then deploy the matching parser/source-rejection runtime and complete hosted acceptance. Local test and browser success does not satisfy this gate.

Full reference parity and customer use remain unverified. The historical callback mismatch is resolved for the tested Google flow; it is retained in the earlier checkpoint as a record of the original blocker.
