# Verification ledger

Recorded 6–8 September 2026 (Europe/Dublin). Secure OpenAI setup and the real synthetic-receipt browser creation/upload/review/approval/JSON/reload workflow are complete. [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md) records the actual persisted result. The current source passed **184 tests** and the production build after adding the requested local billing mock and managed Resend inbox support. [PROVIDER-SETUP.md](PROVIDER-SETUP.md) records the current source identity and exact external prerequisites. The preceding workspace checkpoint fixed parser restoration capacity and atomic membership/invitation audit events. Seven added regressions cover these workspace paths; [WORKSPACE-INTEGRITY.md](WORKSPACE-INTEGRITY.md) records the contract and limits. Prior AI and source-disclosure browser checks remain preserved with their original source identity. A fresh independently authored held-out set measured **49/51 values**; after a date-selection prompt correction, its unchanged-input regression replay measured **51/51**. The original result remains unchanged. The earlier approval and Mac-lock gates are resolved. Resend receiving and Google Sheets setup remain incomplete. The user requested mock billing for now, so real Stripe testing is deferred rather than a current account prerequisite. This ledger does not establish full parity, general document accuracy, production deployment, Resend/Sheets/Stripe account operation or customer use. See [the remaining gates](RELEASE-GATES.md).

## Current evidence

| Layer | Evidence | Current conclusion |
| --- | --- | --- |
| Local database | PostgreSQL 17 on private `.local/socket:55432`; migrations 001–008 applied, including tracked deletion, notifications, intake reservations, reconciliation cursors and the document lifecycle journal | Real persistent local database; no cloud database or managed-auth claim |
| Core API/worker | `tests/core.test.ts`; **20 passed, 0 failed** in the current 184-test aggregate | Local auth, parser/schema, intake, deterministic processing, correction/approval, reprocessing, role/API-key boundaries, invalid uploads, lease recovery, normalizers, origin guard, and deletion scenarios passed. Two added cases prove restore capacity/rollback/configuration preservation and competing restores/creation |
| Membership and invitation audit integrity | `tests/membership-audit.test.ts`; **5 passed, 0 failed**; [workspace contract](WORKSPACE-INTEGRITY.md) | Actual actor/role/tenant events, invitation consumption/revocation/deletion, no-op/rejection suppression, member key/session revocation, retained identities and rollback on audit-write failure passed |
| Provider policies and persistence | `tests/providers.test.ts`; executed `node --import tsx --test --test-concurrency=1 tests/providers.test.ts`; **10 passed, 0 failed, 0 skipped** | Signed webhook receipt, durable dedupe, current-state billing reconciliation, controlled Resend intake, RLS, fixed Sheets ranges, OAuth-state policy passed locally |
| TypeScript | final `npm run build` returned exit 0, including `tsc --noEmit` and Vite production bundle | Type correctness and build output for the final source snapshot; no deployment claim |
| Deterministic extraction evaluation | `scripts/evaluate.ts`, `docs/EXTRACTION-EVALUATION.md`, [frozen lifecycle evaluation](evidence/lifecycle-extraction-evaluation.json); evaluated `2026-09-06T22:43:36.744Z` through the subprocess decoder and deterministic-v2 rules | Historical rules baseline: **54/54** configured leaf values; **54/58** including freeform; scan unavailable in rules mode and malformed PDF rejected. This report is preserved separately from AI evaluation |
| Historical v1 OpenAI regression | [Preserved live report](evidence/openai-evaluation-2026-09-07T08-05-38-093Z.json), 7 September 08:05:38–08:07:05 UTC | **12 actual calls, 74/74 expected leaf values** across native PDF/text, EML/CSV/XLSX/DOCX, freeform, PNG, JPEG, image-only PDF and one document-instruction case. Missing values and evidence warnings remain; malformed PDF was rejected before a call |
| Held-out first pass and current v2 replay | [Original first pass](evidence/openai-heldout-evaluation-2026-09-07T08-30-01-014Z.json); [regression replay](evidence/openai-date-regression-evaluation-2026-09-07T08-34-10-311Z.json) | Original **49/51** (46/47 excluding duplicate); the French date included prose/time in both identical inputs and failed date validation. Prompt v2 selects the calendar component; replay **51/51** (47/47 unique). Malformed PDF rejected, missing date stayed null, review warnings retained. Replay is not new held-out evidence |
| Browser implementation | Root UI acceptance at 1536×1024, 768×1024 and 390×844, with latest lifecycle checks at 1280×720 and 390×844; [BROWSER-QA.md](BROWSER-QA.md), screenshots and actual download byte checks | Central routes, real local upload/review/correction/approval/export, persisted mappings, two-account isolation/viewer roles, responsive navigation and honest provider gates passed; final refinements and scope limits are recorded separately |
| Export/webhook end-to-end | `tests/integrations.test.ts`: **12 passed** in final serial suite | Real exported bytes, immutable approved snapshots, nested mapping, tenant checks, signed controlled retries/replay, exhausted leases and derived deletion passed |
| Source formats | `tests/source-formats.test.ts`: **10 passed** in the current serial suite | Small real JPEG/image-only PDF metadata and original preservation, explicit provider-needed failure, HTML/HTML-only EML text fidelity, plain-body preference and incomplete Office-container rejection passed |
| Intake policies | `tests/intake-policy.test.ts`: **3 passed** in the current serial suite | Archived parser rejection, controlled Resend sender-allowlist handling and expired-session rejection passed; no API-key-expiry claim |
| Document lifecycle | `tests/lifecycle.test.ts`: **7 passed** in the current serial suite | Atomic received/queued history, actual processing/approval/reprocess/failure phases, tenant boundaries, restricted mutation, bounded ordering and cascade deletion passed |
| Export lifecycle | `tests/export-lifecycle.test.ts`: **6 passed** in the current serial suite | Actual render phases, shared operation identity, handled render/persistence failures, prior-approval exports preserving newer review state, tenant rejection and deletion passed |
| AI extraction / OCR | `server/core/openai-provider.ts`, explicit entrypoint initialization, **18 provider + 9 worker + 9 CSV + 2 written-date tests** in the current aggregate; [AI contract](AI-EXTRACTION.md) | Configured native implementation with schema-constrained text/PDF/image extraction, pinned model, raw/normalized separation, qualified evidence, bounded failure handling and stored tokens/cost. Controlled tests and real calls pass within their documented scope; the actual synthetic PNG browser workflow passed source review, approval, JSON download and persisted reload; [exact evidence](AI-BROWSER-ACCEPTANCE.md) |
| Review provenance and conditional UI | [REVIEW-PROVENANCE.md](REVIEW-PROVENANCE.md); 8 original-source, 4 onboarding availability and 3 run-metadata rendering tests | Nested/table quotes are exposed through immutable original-row disclosures; corrections cannot relabel source rows. Availability/error/absent-cost states pass controlled rendering. Browser table disclosures, complete table-level quotes, immutable original rows after a temporary edit, keyboard operation, desktop/mobile page navigation and mobile Document switching passed after the overflow fix; deeper recursion and structural row edits retain controlled-test evidence |
| Credential boundary | [Current redacted check](evidence/review-provenance-key-boundary.json): configured true, environment file mode 600, Git ignored/untracked, 174 client files scanned with no key match | Server configuration and this bounded output scan pass; no key material is included in evidence and no exhaustive secret-audit claim is made |
| Resend | Custom-domain verification plus managed-inbox probe support; seven managed tests and the added provider-route case pass | Account access and the managed domain are observed; key creation, probe sending/retrieval, signed webhook delivery and real intake remain pending |
| Google Sheets | OAuth state/token/range adapter plus controlled transports; local export workbook prepared | Only a private empty Drive folder exists. Native Sheet import and Cloud project creation are blocked; the native picker fallback also encountered a new 8 September Mac lock; no real OAuth consent, refresh, delivery or revocation verified |
| Billing | Explicit development-only mock; six backend and three UI rendering tests pass; separate Stripe test adapter retained | User requested mock billing. Real local plan/limit persistence, audit, rollback, access controls and Stripe-transport exclusion pass controlled tests; the [browser mock workflow](evidence/provider-setup-2026-09-08/billing-browser.json) also passed selection/change/cancel/reload with eight documents preserved. Actual Stripe testing is deferred |
| Production / users | No deployed revision, managed storage/auth, account recovery, provider launch, or customer-use evidence | Local development evidence only |

The preceding `npm test` aggregate passed **160/160, 0 failures and 0 skipped**, from `2026-09-07T09:09:18.914692Z` to `09:09:44.685551Z`; build passed from `09:09:44.686219Z` to `09:09:46.410534Z`. [Test output](evidence/review-provenance-tests-results.txt), [build output](evidence/review-provenance-build-results.txt) and [integrity record](evidence/review-provenance-final-integrity.json) identify **185 files**, fingerprint `f2bd05a30bd9e4c0f891c6b908e197bd8fd54cbc46f9919563be869c10b002f8`, unchanged at `09:11:09.186169Z`. The separate worker was stopped during database tests. The 110 compared extraction/backend/schema/fixture/config files are unchanged from the prior v2 AI proof; no new provider calls were needed for this frontend change. Historical 145-test and AI reports remain intact.

The historical lifecycle aggregate passed **107/107** on 6 September. Its breakdown was bearer API workflow 5, automation 2, core 18, decoder/reconciliation 9, defensive regressions 5, extraction 12, correction/approval reconciliation 1, frontend-value helpers 4, integrations 12, providers 9, retention/notifications 4, source formats 10, intake policies 3, document lifecycle 7 and export lifecycle 6. The [historical manifest](evidence/verification-lifecycle-run.json) identifies that earlier source; it is not proof for later OpenAI changes.

## Current provider-setup source — 8 September 2026

The complete suite passed **184/184 tests**, zero failures/skips, from `2026-09-08T20:27:19.867548Z` to `20:27:50.185332Z`; TypeScript/Vite build passed from `20:27:50.186090Z` to `20:27:52.235746Z`. [Test output](evidence/provider-setup-2026-09-08/tests-results.txt), [test timing](evidence/provider-setup-2026-09-08/tests-run.json), [build output](evidence/provider-setup-2026-09-08/build-results.txt) and [build timing](evidence/provider-setup-2026-09-08/build-run.json) retain the actual commands and exits. [Verification](evidence/provider-setup-2026-09-08/verification.json) and [source manifest](evidence/provider-setup-2026-09-08/source-before.json) identify **191 files**, fingerprint `a1b67de414f66a5724ead433ff286f28620a77014a8f41f0f331cebef9de99e0`, unchanged during verification. The 17 additional cases comprise six mock backend tests, three mock UI tests, seven managed-inbox tests and one managed email-route case in the now ten-test provider suite.

[Provider setup](PROVIDER-SETUP.md) separates account access and prepared artifacts from completed integrations. Local mock billing is enabled and real Stripe verification is deferred by the user. Resend key creation and the synthetic probe are unapproved/unsent; Google project creation was rejected and no native spreadsheet exists. The [billing browser workflow](evidence/provider-setup-2026-09-08/billing-browser.json) passed Standard → Team → cancel to Explore → restore Team → reload. [Runtime/DB readback](evidence/provider-setup-2026-09-08/runtime-and-billing-state.json) confirms the Team mock allowance, 39 used pages, eight preserved documents, four matching audit events and zero subscription/Checkout rows. Desktop 1280×720 and mobile 390×844 layouts had matching document/body widths; mobile mutations were not tested. The viewport was reset and captured browser warnings/errors were empty. Web/API health and `/api/presets` returned HTTP 200 at `2026-09-08T20:33:22.739Z`; the earlier diagnostic `/api/public/presets` 404 is retained as an incorrect route probe. Earlier AI calls and browser results are preserved; the new controlled tests make no provider requests.

## Historical workspace-integrity source — 7 September 2026

The 7 September source passed **167/167 tests**, zero failures/skips, from `2026-09-07T17:30:36.845509Z` to `17:31:00.277777Z`; TypeScript/Vite build passed from `17:31:00.278492Z` to `17:31:01.952573Z`. [Test output](evidence/workspace-integrity-tests-results.txt), [test timing](evidence/workspace-integrity-tests-run.json), [build output](evidence/workspace-integrity-build-results.txt) and [build timing](evidence/workspace-integrity-build-run.json) are preserved. The [final integrity record](evidence/workspace-integrity-final.json) identifies **186 files**, fingerprint `a4641915d5042524908c4e98efe14945ac15df13923f27c9307f7733264ed4f0`, unchanged during verification. Only parser/workspace routes and the two associated test files changed from the previous AI-browser source checkpoint. Extraction/model code and the existing UI are unchanged; no additional model calls were needed.

The focused [two parser-capacity tests](evidence/parser-quota-targeted-results.txt) and [five membership-audit tests](evidence/membership-audit-targeted-results.txt) passed before the full aggregate. Four safe Request failed logs in the latter are deliberate audit-insertion faults; the rollback assertions passed. [WORKSPACE-INTEGRITY.md](WORKSPACE-INTEGRITY.md) records the cooperating-route concurrency boundary, bounded audit metadata, no-op behavior and exact limitations. No additional provider request, customer evidence or browser race simulation is inferred.

## What the automated checks establish

Core fixtures create two independently authenticated accounts and workspaces, store real password/session records, save a versioned parser, upload a real synthetic original, and run the durable text-anchor worker. Assertions check invoice identifiers/amounts/line items and actual page evidence; duplicate content reserves usage once; unauthorized documents are unavailable through routes and RLS. They also exercise stale correction/approval revisions, preservation of earlier approved runs, manual invitation acceptance, viewer limits, scoped-key revocation, malformed/empty/unsupported uploads, explicit scan failure, a recovered expired job lease, saved template/nonmatching-template behavior, numeric/date/boolean normalization, cross-origin rejection, and original/result deletion.

Provider fixtures create unique local tenant records and use controlled external transports. Stripe raw signatures reject tampering, old signatures, and live events; a repeated signed HTTP event has one durable row. Reconciliation retrieves a controlled current subscription, so a later arriving older event cannot restore canceled access. Metadata and unmapped or mismatched prices do not grant a paid plan. Resend verifies raw signatures, stores minimal pointers, routes only matching signed/retrieved provider `received_for` values, reads body and attachment separately, and sends both through real core intake twice without duplicate documents or usage. Sheets tests preserve literal formulas/leading zeros with RAW range requests, reserve one range under concurrent duplicate calls, and deny cross-tenant reservations. OAuth tests reject the wrong user, expired/replayed state, and state invalidated by disconnect.

The first provider run found an installed-SDK incompatibility: Svix 2 verifies the signature but returns no parsed object. The adapter now verifies first and explicitly parses JSON afterward; the rerun passed. The initial sandbox runs could not connect to local sockets. The passing run used approved local socket access; this was an execution-environment issue rather than an application test result.

## Historical deterministic extraction evaluation scope

| Fixture class | Measured result |
| --- | --- |
| Two-page native PDF, continuing labelled line items | 23/23 leaf values |
| German decimals and day-first dates | 11/11 |
| Missing required data | 4/4 expected values, including nulls; review issues preserved |
| EML lead email | 4/4 |
| Labelled/quoted CSV | 4/4 |
| Labelled XLSX receipt | 4/4 |
| Labelled DOCX receipt | 4/4 |
| Unconfigured freeform receipt | 0/4; missing fields reported for review |
| Image scan | No readable rule text; provider-required state, not a successful extraction |
| Malformed PDF | Rejected before processing |

The held-out fixture files differ from the onboarding samples, but their supported layouts deliberately follow field labels and table headers that the deterministic engine can recognize. Counts include expected missing values and are small synthetic measurements, not general accuracy percentages. Freeform and scan failures are retained in the report. The separate source-format tests and browser checks below establish bounded HTML intake behavior; HTML is not included in these 54/54 leaf-value counts. Arbitrary customer layouts, general HTML/CSS rendering fidelity, merged or complex nested tables, broad OCR languages, noisy scans, and large-load behavior remain unproven. No AI provider was invoked in this deterministic evaluation; later real OpenAI results are recorded separately. Raw discrepancies and issues are retained in the JSON evidence.

## Browser QA

Root has recorded real UI actions and screenshots in [BROWSER-QA.md](BROWSER-QA.md). The sample and independently uploaded two-page PDF passed intake, persistent worker processing, source review, corrections, approval and real CSV/XLSX/JSON downloads. Saved column mappings and enum corrections survive reload. A second account demonstrated workspace denial, manual invitation acceptance and viewer restrictions. Mobile/tablet/desktop navigation, lower marketing sections, help and legal pages were inspected. The historical unconfigured scan check correctly failed with an OCR/provider-required message. Those deterministic actions remain separate from the later [completed AI receipt browser path](AI-BROWSER-ACCEPTANCE.md), which records actual image comparison, approval, JSON bytes, model/prompt/tokens, usage and all seven lifecycle events after reload.

[Browser download evidence](evidence/browser-downloads.json) compares actual downloaded bytes with stored approved snapshots and original fixture bytes. The current [design fidelity review](design/FIDELITY-REVIEW.md) retains concrete layout differences. New compact-schema/notification refinements and final console observations are separate acceptance entries. The continuation exercised 26 documents across three pages and the pagination/deletion boundaries. Large-load behavior, arbitrary real documents and every possible setting combination are not established by this browser pass.

The later lifecycle browser pass uploaded a synthetic German receipt, displayed Merchant `Linden Küche`, Date `2026-09-17`, Currency `EUR` and Total `1234.56`, then approved and downloaded [the actual JSON export](evidence/approved-locale-receipt.json) with those exact values and no correction. [Lifecycle DOM evidence](evidence/lifecycle-browser.json) and the [approved-export screenshot](evidence/lifecycle-approved-export.png) show all seven actual phases: received, queued, processing, ready for review, approved, export started and export completed. A separately uploaded flat HTML table yielded Merchant `Cedar & Pine`, Total `18.6` and Currency `EUR`; Date stayed empty with the required-field issue. See [HTML values/source evidence](evidence/html-browser-values.json) and [review screenshot](evidence/review-html-table.png). This HTML check does not claim approval or a filled missing date.

At 390×844, the lifecycle History view had a 390-pixel document width and all seven events were accessible through actual scrolling, followed by selected-run provenance and the action footer. Canonical viewport screenshots are [initial History](evidence/lifecycle-mobile.png) and [scrolled History](evidence/lifecycle-mobile-scrolled.png), with [initial DOM evidence](evidence/lifecycle-mobile.json) and [scrolled DOM evidence](evidence/lifecycle-mobile-scrolled.json). After restoring 1280×720, the [captured tab console](evidence/browser-console-lifecycle.json) had no error/warning entries. This is local synthetic browser evidence, separate from the source-stable test/build run.

## AI receipt and review-source browser completion — 7 September 2026

The user unlocked the Mac and explicitly approved parser creation. The real browser workflow now proves AI receipt intake, worker extraction, image comparison, approval without correction, exact JSON bytes and persisted reload with all seven lifecycle events. [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md) records the four matching values, raw total, model/prompt/tokens and estimated usage. [Machine-readable acceptance](evidence/ai-browser-acceptance.json) preserves IDs, download hash, browser assertions and limits.

Browser verification passed at **1280×720** and **390×844**. Desktop document/body widths were 1280px; after the targeted CSS fix mobile widths were both 390px, compared with the retained earlier 422px document overflow. Source buttons were at least 44px high. Native disclosure Space toggling, Tab focus and Enter activation passed. Page-two quotations navigated to page two; mobile source activation selected the Document tab, with page-one navigation checked too. The original four row labels and complete table-level quotations were present, while absent leaf quotations remained explicit. A temporary unsaved Description edit left the original disclosure text unchanged and was restored without saving. Nested objects, deeper recursion and structural row edits retain controlled component evidence; this browser pass did not create a fixture for every depth. The final browser warning/error read was empty and the temporary viewport was reset. [Review provenance](REVIEW-PROVENANCE.md) links the captures and retained pre-fix measurements.

The preceding AI-browser source passed **160/160 tests**, zero failures/skips, from `2026-09-07T17:15:22.744417Z` to `17:16:03.468224Z`; TypeScript/Vite build passed from `17:16:03.469913Z` to `17:16:05.141937Z`. [Test output](evidence/ai-browser-tests-results.txt), [test timing](evidence/ai-browser-tests-run.json), [build output](evidence/ai-browser-build-results.txt) and [build timing](evidence/ai-browser-build-run.json) are preserved. The [final integrity record](evidence/ai-browser-final-integrity.json) identifies **185 files**, fingerprint `d58b8bbac1a945458f955b204da5499273de6ef12531636d809ca11f27f736c4`, unchanged during verification. Only `src/features/documents/review.css` changed from the preceding source-disclosure checkpoint: its table wrapper now contains the absolutely positioned screen-reader label, eliminating the observed mobile page overflow.

## Reproduce and extend evidence

Run from the project root with the database migrated and a separately running development worker stopped while tests own queue fixtures:

```sh
npm run setup
npm run typecheck
npm test
node --import tsx scripts/evaluate.ts
npm run build
```

Tests should remain serial across files while using the same database. Evaluation writes the documented Markdown/JSON outputs and original generated files. Tests must retain their own workspace/data cleanup. Do not substitute a production customer database for the local fixture database.

## Final verification additions

Root browser evidence is linked above. Each later addition retains its command/workflow, date, result, material limitation and artifact path. Real AI, email, Google account, Stripe test-mode, deployment and customer use remain separate evidence layers.

### Earlier 63-test backend/build evidence — 6 September 2026

- `npm test`: **63/63 passed**, 0 failures, 0 skipped; started `2026-09-06T21:35:32.800014Z`, finished `21:35:42.733150Z`; exact output [final-test-results.txt](evidence/final-test-results.txt).
- `npm run build`: exit **0**; TypeScript and Vite production bundle passed; started `2026-09-06T21:36:55.380700Z`, finished `21:36:57.301671Z`; exact output [final-build-results.txt](evidence/final-build-results.txt). This produced a local bundle, not a deployment.
- `node --import tsx scripts/evaluate.ts`: exit **0**, report generated `2026-09-06T21:34:34.635Z`; configured layouts 54/54 leaf values; all readable layouts including deliberate freeform mismatch 54/58. No AI provider invocation. The final suite ran after this command regenerated the fixture files; its fixture hashes therefore match the final manifest.
- [AUTOMATION-RECIPES.md](AUTOMATION-RECIPES.md) and fixture JSON/schema cover Zapier, Make, n8n and Power Automate bridge mappings. Tests validate the actual queued approval payload, literal identifiers, signature-before-parse receiver behavior and stable replay identity. No native marketplace install or real vendor account flow was performed.

The bounded defensive pass fixed default line-item expansion, final-attempt webhook lease recovery, stable encryption-key creation across processes, explicit approved export selection and deletion serialization, correction timestamps after locks, strict ambiguous-number handling, and tenant-scoped tracked file cleanup. Five targeted regression tests prove duplicate-event binding, changed-replay rejection, deleted-event tombstones, failed file removal/retry and text enum validation. These checks used owned local fixtures and controlled transports; no external exploitation or production data was involved.

Before the earlier 50-test serial suite, the only pending browser job was verified by exact SHA-256 against `fixtures/generated/invoice-multipage.pdf` and processed by its explicit job ID. It reached `needs_review` with two source pages. Its saved template did not match, so the engine surfaced the documented fallback warning and used field-label anchors. This was real deterministic processing, not seeded output.

The final source uses a bounded decoder subprocess and durable interrupted-intake recovery. Nine focused regressions establish real PDF decoding, environment allowlisting, heap arguments, input/output/deadline/concurrency controls, reference/live-lease preservation, cursor progress beyond 100 originals and expired no-file reservation cleanup. [DECODER-RECOVERY.md](DECODER-RECOVERY.md) records exact limits. The subprocess still has the OS user’s filesystem/network privileges; OS/container restrictions and CPU/full-memory/process quotas remain deployment gates. Directory metadata enumeration is linear. Unattended retention/crash/load soak and production backup/restore are not established. Originals removed from Folio do not revoke copies already downloaded or sent to external destinations.

### Earlier source identity and historical evidence

[verification-final-run.json](evidence/verification-final-run.json) records 119 source/config/test/fixture/public files and separately timestamped test, evaluation and build results. Its SHA-256 fingerprint is `7acc735a5ddbd73002bbd06ab15443082a3423c95a0c637357c2f81f7b83fd4c`. The exact file list was checked unchanged from the final test start through post-build verification at `2026-09-06T21:38:06.102000Z`. Documentation, generated `dist`, installed dependencies, runtime data and secrets are outside that source fingerprint; the manifest states its scope and algorithm.

The earlier [verification-run.json](evidence/verification-run.json), [50-test output](evidence/test-results.txt), and [earlier build output](evidence/build-results.txt) remain unchanged as historical evidence. They describe earlier source. The 63-test record, 81-test continuation and 107-test lifecycle verification are historical beside the later OpenAI verification. Evaluation regenerates its general report path and binary fixtures, so old recorded hashes identify their earlier versions; preserved command outputs remain the historical evidence. The intervening frontend-only build remains separately recorded; it is not used as proof that the earlier tests covered later UI. Browser actions and screenshots can be updated independently without changing executable-source identity.

### Configured retention and processing notifications — 6 September 2026

`node --import tsx --test --test-concurrency=1 tests/retention-notifications.test.ts` passed **4/4**, 0 failures and 0 skipped after a test-only SQL parameter cast fix. Migrations 005/006 applied and TypeScript passed. Owned two-workspace fixtures call the actual age-expiry maintenance function with a workspace restriction; aged terminal files and all local derived copies/read receipts disappear, while fresh/active/other-workspace data remain. Read actions preserve exact documents/runs/jobs; preference and personal read state persist. This proves age selection and deletion behavior, not an unattended multi-day timer soak.

[NOTIFICATIONS-RETENTION.md](NOTIFICATIONS-RETENTION.md) records the event/read/deletion contracts and limits. The [browser pass](BROWSER-QA.md#processing-notifications) verified disabled/enabled states, five real outcomes, read persistence, document navigation, 390-pixel fit and Escape focus restoration. The setting now has implemented and verified in-app behavior; notification emails are not implemented.

### Scoped bearer API workflow — 6 September 2026

`node --import tsx --test --test-concurrency=1 tests/api-workflow.test.ts` passed **5/5**, 0 failures and 0 skipped; `npm run typecheck` also passed. The suite constructs the actual `buildApp()` Fastify server and registers two owned local accounts. It creates scoped keys through the session route, sends a real multipart request with a synthetic original and idempotency header, reads the queued job, invokes only `processOneCoreJob(returnedJobId)`, and retrieves the completed job/document/run through bearer endpoints. Actual original bytes, digest, schema version, page evidence, leading-zero identifier, currency number, false boolean, missing null, and raw/normalized separation are asserted.

The HTTP replay and an independently keyed same-content upload preserve one document, one processing job and one page reservation. Changed content with the same key returns409. Read-only/results-only keys receive the expected403 on unsupported operations; a valid key from another workspace gets404 for these resources and cannot upload into this parser. Revocation returns401 for upload/job/document/original/run requests while the owner session still retrieves the stored result. All owned rows/files are removed afterward. There are no global queue claims, delivery-worker calls or provider requests.

The run emitted the existing Fastify `FSTDEP023` warning about deprecated `disableRequestLogging`; it did not fail a test. This verifies the documented local HTTP contract using injection and the real decoder/database/worker, not a network deployment, external automation account or API load test. The former browser-ledger claim of automated pagination-boundary coverage was retracted; root records actual pagination actions separately.

### Earlier 81-test continuation: multiline, API and browser completion checks

The rules engine now records **deterministic-v2**. A multiline field captures its first anchored block on one page, stopping at a blank line, effective schema/template anchor, known CSV header, or pipe/tab structure. Internal newlines, raw values and exact source evidence remain distinct from normalized/corrected values. A block exceeding 100 value lines or 65,536 UTF-16 units returns null with a review issue. This is bounded labelled-text extraction; it does not recover arbitrary page-spanning messages or general freeform content.

Twelve pure extraction tests cover those boundaries, empty/default behavior, raw-preserving trim/case transforms and precise line-item/subtotal/tax mismatch issues. One scoped HTTP/database regression proves mismatched values reject approval without creating an approval, correction permits approval, and original normalized/raw values remain immutable. The independent review found no actionable issues. Five bearer API cases are described above. The onboarding fix preserves a nonblank custom parser name when the schema preset changes.

- **81/81 tests passed**, zero failures/skips, from `22:06:18.808394Z` to `22:06:30.009266Z`: [exact output](evidence/continuation-test-results.txt).
- **Production build passed**, exit 0, from `22:06:49.177376Z` to `22:06:50.946112Z`: [exact output](evidence/continuation-build-results.txt).
- **Evaluation passed** under deterministic-v2 at `22:05:27.168Z`: 54/54 supported leaf values, 54/58 including freeform, scan requires provider, malformed PDF rejected. [Frozen report](evidence/continuation-extraction-evaluation.json). No AI call.

[verification-continuation-run.json](evidence/verification-continuation-run.json) hashes **152** source/config/test/fixture/public files, unchanged between test start and post-build verification at `22:07:24.383615Z`. Fingerprint: `e67237c39dcc349b54d4956cc2b3729050974acfca890871fe092a715036b60a`. Documentation, installed dependencies, dist, runtime data and secrets are outside that declared scope. This 81-test manifest and the earlier 63/50-test manifests describe their recorded source snapshots; all remain historical beside later verification.

After restarting the worker, the browser uploaded the original `fixtures/browser-multiline/lead-message.txt` into a stock Lead emails parser. Review displayed all three message lines, separate Company, and deterministic-v2 provenance. Approval and JSON export persisted after reload as Run 1 · Approved / Exported. The actual downloaded [approved-multiline.json](evidence/approved-multiline.json) matches all four expected values exactly, including newlines and email, with no correction. Its 575 bytes hash to `da492821d480569ed60d9f1114c8c5b7508e0d790679d5d713b33d888b082d31`. [BROWSER-QA.md](BROWSER-QA.md) also records pagination, membership/invitation/template controls and a synthetic webhook management fixture; no outbound request was made. The browser error/warning log captured for that continuation was empty.


### Historical 107-test lifecycle verification — 6 September 2026

- `npm test`: **107/107 passed**, 0 failures and 0 skipped; started `2026-09-06T22:44:24.273055Z`, finished `22:44:45.209463Z`; [exact test output](evidence/lifecycle-test-results.txt).
- `npm run build`: exit **0**, including TypeScript and Vite production bundle; started `2026-09-06T22:44:56.418472Z`, finished `22:44:58.325104Z`; [exact build output](evidence/lifecycle-build-results.txt). This is a local build.
- `node --import tsx scripts/evaluate.ts`: exit **0**; started `2026-09-06T22:43:33.224013Z`, finished `22:43:36.754770Z`; [exact evaluation output](evidence/lifecycle-evaluation-results.txt). The [frozen report](evidence/lifecycle-extraction-evaluation.json), generated at `22:43:36.744Z`, records deterministic-v2 with **54/54** supported leaf values and **54/58** across readable layouts. The freeform mismatch, provider-required scan and malformed-PDF rejection remain visible. No AI provider was invoked.

[verification-lifecycle-run.json](evidence/verification-lifecycle-run.json), recorded at `2026-09-06T22:45:32.498750Z`, identifies **169** source/config/test/example/fixture/public files. Its fingerprint is `2ab5de77522d89c03f5dd82ce03c0ff634ac6add018978f9a0a1a6c104acb3c4`. The recorded source was unchanged between test start and post-build verification; documentation, dist, installed dependencies, runtime data and secrets are excluded by the declared scope. Migrations 001–008 were applied. The previous 81-test continuation keeps its own manifest and [frozen evaluation report](evidence/continuation-extraction-evaluation.json); its tests are not presented as proof for later source.

The 26 additional tests cover source formats (10), intake policies (3), document lifecycle (7) and export lifecycle (6). JPEG and image-only PDF fixtures preserve readable metadata and byte-identical private originals through actual scoped intake and terminal provider-needed processing; they produce no invented OCR or successful run. HTML tests preserve entities, separate lines/list items, long scalar values, heading case, visible link text and flat table boundaries while omitting script/style content. HTML-only EML uses the same conversion, and an actual plain-text body takes precedence. Harmless incomplete DOCX/XLSX containers are rejected. These fixtures do not establish general layout fidelity, OCR, or a sanitizer security guarantee.

Intake-policy tests reject archived-parser uploads/samples and expired sessions without acceptance side effects, while a fresh session remains valid. The controlled Resend allowlist fixture verifies rejection and actionable handling before body/attachment intake, plus an allowed positive control. It does not prove a live inbox, and optional API-key expiration is not implemented.

Lifecycle tests prove received and queued events commit with real job creation in one transaction, failed acceptance rolls back its journal, actual processing/approval/reprocess/failure phases retain operation identity, unchanged/duplicate operations add no invented phase, and public history is newest-first, bounded to 200 and hides its ordering sequence. Tenant isolation, direct event mutation denial and document-delete cascade are asserted. Export tests prove rendering and snapshot persistence are bracketed by events with one operation identity, handled render/format/persistence failures retain safe failed attempts without partial snapshots, and exporting an earlier approval preserves a newer review state. Invalid selections create no export phases. These are owned local fixtures and controlled failures, not concurrent production stress or abrupt-process-loss recovery evidence. [DOCUMENT-LIFECYCLE.md](DOCUMENT-LIFECYCLE.md) records the synchronous transaction and crash limitations.

The [final integrity check](evidence/lifecycle-final-integrity.json) at `2026-09-06T22:58:24.484759Z` confirmed the same 169-file source fingerprint and matching test/build/evaluation outputs, frozen evaluation report and downloaded locale JSON hashes.

### Current OpenAI verification — 7 September 2026

Secure credential setup resolved the previous hard stop. The entrypoints now initialize the native Responses API adapter with the pinned `gpt-5.4-mini-2026-03-17` model and `folio-openai-extraction-v1` prompt. Every PDF sends its original plus decoded page text; PNG/JPEG send original image bytes; other supported formats send decoded text. Strict required/nullable schemas and local recursive validation reject missing/unknown keys or invalid structures. Missing extracted values stay null before documented defaults; normalization preserves raw values. A native quote and raw scalar must occur on the cited page; model-read visual quotes are explicitly qualified and require review. These checks do not establish semantic field association or independent visual verification. [AI-EXTRACTION.md](AI-EXTRACTION.md) records resource bounds, error categories, cancellation, model/token/cost provenance and provider-data limitations.

| Evidence | Exact result |
| --- | --- |
| Historical v1 aggregate | [145-test output](evidence/openai-final-tests-results.txt), [timing](evidence/openai-final-tests-run.json): `npm test`, **145 passed, 0 failed, 0 skipped**, `08:05:33.986510Z`–`08:05:56.362225Z` |
| Historical v1 build | [Build output](evidence/openai-final-build-results.txt), [timing](evidence/openai-final-build-run.json): `npm run build`, exit **0**, TypeScript and Vite passed, `08:05:56.362826Z`–`08:05:58.008198Z` |
| Source identity | [180-file manifest](evidence/openai-final-source-before.json), [integrity check](evidence/openai-final-integrity.json): SHA-256 `8d8b81756ef5e2bd79182997d956feb104cc21936c66f84d339390ec95c6f207`, unchanged at `08:11:42.443485Z` |
| Fresh real evaluation | [Report](evidence/openai-evaluation-2026-09-07T08-05-38-093Z.json): `08:05:38.093Z`–`08:07:05.124Z`, **12 actual calls and 74/74 expected leaf values**, malformed PDF rejected before a call, estimated successful-call cost **$0.017121 USD** |
| Server credential boundary | [Redacted check](evidence/openai-key-boundary-check.json): configured, local environment mode **600**, **172** client files scanned with no key match; Git-ignored server environment retained locally |

The historical AI-era 145-test aggregate added 18 controlled adapter, 9 controlled worker, 9 CSV and 2 written-date cases to the 107-test baseline. Adapter tests cover strict nested shapes, wrong model, refusal/incomplete/HTTP errors, input/output bounds, timeout, source mismatch and CSV normalization. Worker fixtures use exact owned jobs and verify configured/unconfigured isolation, pinned schema/instructions/locale, retry/permanent/deadline/shutdown behavior, provenance and preserved originals/approvals. CSV decoding is source-aware: only complete quoted CSV cell tokens are decoded for normalization, including embedded comma/newline/doubled quotes; malformed input and ambiguous or already-decoded quoted values remain unchanged. Raw values and evidence are preserved.

The live fixture result includes 23/23 native multi-page invoice values, 11/11 German invoice values and ten 4/4 cases covering missing data, EML, CSV, XLSX, DOCX, freeform text, PNG, JPEG, image-only PDF and one bounded document-instruction input. Every successful result remains `needs_review`. Missing dates in JPEG/image-only PDF remain null with required-field issues. PNG/JPEG retain visual-evidence warnings; the final image-only PDF result has missing-evidence issues despite correct expected values. The freeform call took **57.011 seconds**, so this is not a low-latency guarantee. These iterated synthetic fixtures informed written-date/CSV fixes and constitute a regression evaluation, not a held-out accuracy benchmark. Counts include expected nulls and do not establish general accuracy or prompt-injection resistance. Costs estimate successful responses only, not the provider bill; failed/timed-out/discarded attempts may incur unrecorded charges.

Historical results remain unchanged:

- [Initial real freeform probe](evidence/openai-evaluation-2026-09-07T07-30-29-792Z.json): **2/4** values. The source actually says `OAK MARKET`; [the AI expectation sidecar](../fixtures/ai/expectations.json) explains the historical `Oak Market` oracle correction. Written-month normalization was a separate implementation fix; the original manifest and failed report remain intact.
- [First full real run](evidence/openai-evaluation-2026-09-07T07-40-37-066Z.json): **9 calls, 62/62** values, with evidence warnings retained.
- [Expanded real run](evidence/openai-evaluation-2026-09-07T07-46-30-870Z.json): **12 calls, 73/74** values. CSV merchant container quotes survived normalization; this failure prompted the bounded source-aware fix and is not rewritten as a pass.
- [Earlier 135-test output](evidence/openai-tests-results.txt), [test timing](evidence/openai-tests-run.json), [build output](evidence/openai-build-results.txt) and [build timing](evidence/openai-build-run.json) describe source before the CSV follow-up. The 145-test record supersedes them for current source, without deleting them.

Historical approval checkpoint: automatic approval review rejected the browser **Create AI receipt QA** action. [The prepared form](evidence/ai-parser-pending-approval.png) records the configured AI option, name and receipt preset before submission. At that checkpoint the positive browser workflow was pending explicit approval; no completed AI parser or result was claimed. [Local runtime checks](evidence/openai-local-runtime.json) returned HTTP 200 for web/API/presets with AI configured, and the worker restarted after the aggregate. The goal subsequently became blocked by approval/Mac lock, then resumed after the user explicitly approved creation and unlocked the Mac. The [completed AI workflow](AI-BROWSER-ACCEPTANCE.md) now supplies direct browser evidence. Resend receiving, Google Sheets, Stripe test Checkout/portal, production authentication/deployment and customer use retain distinct gates.


## Held-out evaluation and current date-selection correction

The [independent authoring record](evidence/heldout-ai-2026-09-07/authoring-verification.json) and [manifest](evidence/heldout-ai-2026-09-07/manifest.json) were finalized before any model call. The author read only the public schema types/presets and installed-library list, without extraction code, existing tests/fixtures or reports. Original bytes, expectations, runner and v1 source were hashed before evaluation. Five unique valid documents cover a two-page six-row invoice, French image receipt, image-only PDF with no date, narrative email with paragraph breaks and freeform English receipt; an exact image duplicate and malformed PDF complete the set.

The [first pass](evidence/openai-heldout-evaluation-2026-09-07T08-30-01-014Z.json) remains **49/51**: both copies of the French receipt included time or surrounding text in their raw date, so strict date validation kept them in review. The unique-document result is **46/47**. Missing dates stayed null and malformed PDF was rejected before a provider call. Duplicate model input checks consistency only; actual intake deduplication remains covered by separate local tests.

Only `openai-provider.ts` changed afterward: prompt version v2 adds date-field selection instructions. Raw values and strict normalization remain intact. The [regression replay](evidence/openai-date-regression-evaluation-2026-09-07T08-34-10-311Z.json) measured **51/51** across six calls (**47/47** excluding duplicate), with visual/required-field issues retained. Its estimated successful-call cost is **$0.01604925 USD**, not actual billing reconciliation. No further prompt/code changes occurred between replay cases. This is a regression result after seeing the first-pass failures, not a second held-out result. The current test/build source proof is linked above; v1 reports and earlier failures remain unchanged. The later [AI browser approval/export](AI-BROWSER-ACCEPTANCE.md), other live providers and production are separate evidence layers.


Historical v2 handoff checkpoint: [v2 runtime](evidence/openai-date-local-runtime.json) confirms HTTP 200 web/API/presets and observed worker startup after tests; [redacted credential check](evidence/openai-date-key-boundary-check.json) confirms mode 600, Git ignored/untracked and no key match across 172 client files. At that checkpoint the AI browser workflow awaited explicit approval; the later completed workflow is recorded in [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md).


## Current review-source acceptance gate

[REVIEW-PROVENANCE.md](REVIEW-PROVENANCE.md) records the source-disclosure fix and 15 controlled rendering cases included in the 160-test checkpoint. [Passive browser evidence](evidence/ai-passive-browser-check.json) confirms the prepared AI form at 390×844 and Usage at 1280×720 before that disclosure. CUA then reported a locked Mac and failed automatic unlock; disclosure checks, viewport reset and the rejected AI creation action remained pending at that historical checkpoint. The user subsequently unlocked the Mac and explicitly approved creation. The [actual AI browser workflow](AI-BROWSER-ACCEPTANCE.md) is now complete; the completed disclosure/mobile/keyboard acceptance is recorded in [REVIEW-PROVENANCE.md](REVIEW-PROVENANCE.md).

The [runtime/configuration checkpoint](evidence/review-provenance-runtime.json) confirms local HTTP 200 and worker restart after the 160-test run. OpenAI was configured; required Resend, Google OAuth and Stripe test settings were absent. The Mac lock at that checkpoint was later resolved by the user; it is no longer an active browser prerequisite.

After the final test/build run, the API, worker and Vite services were restarted. Browser reload again showed **Run 1 · Approved**, **Exported** and the same four values. The final 1280×720 viewport had a 1280px document width and no captured browser warning/error entries. [After-restart DOM](evidence/ai-browser-after-restart.txt), [screenshot](evidence/ai-browser-after-restart.png), [acceptance record](evidence/ai-browser-acceptance.json).

The historical [redacted runtime/configuration check](evidence/ai-browser-runtime.json), recorded `2026-09-07T17:19:42.405Z`, confirms web/API health/presets HTTP 200, observed worker startup and OpenAI configuration. At that checkpoint Stripe test, Resend and Google OAuth settings were absent, with Resend inbound disabled. No credential values were printed.


After the 7 September 167-test/build run, the local services restarted and a read-only browser reload confirmed the existing AI receipt remained **Run 1 · Approved / Exported**. Document/body widths were 1280px at the default 1280×720 viewport; captured browser warnings/errors were empty. [Browser checkpoint](evidence/workspace-integrity-browser.json), [DOM](evidence/workspace-integrity-browser.txt), [screenshot](evidence/workspace-integrity-browser.png). This is persistence/smoke evidence, not a new browser membership or parser-capacity mutation test. The [historical redacted runtime check](evidence/workspace-integrity-runtime.json), `2026-09-07T17:33:01.314Z`, confirms web/API health/presets HTTP 200 and observed API/worker/Vite startup. At that checkpoint OpenAI was configured; Stripe test, Resend and Google OAuth settings were absent, with Resend inbound disabled.
