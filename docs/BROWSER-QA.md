# Browser acceptance evidence

Local browser verification on 6–7 September 2026 against `http://127.0.0.1:5178`, with Fastify on port 4318 and PostgreSQL. The earlier workflows used synthetic originals and deterministic extraction. After explicit user approval and Mac unlock, the browser also completed the **AI receipt QA** create/upload/worker extraction/source review/approval/JSON/reload workflow. [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md) records the result separately from real model regression evaluations. New source-disclosure desktop/mobile/keyboard checks passed after a targeted CSS overflow fix. No external customer account, other provider delivery or production deployment is claimed. Earlier desktop was 1536×1024, tablet 768×1024 and mobile 390×844; the latest desktop checks used 1280×720. The separate design review compares native screenshots with the generated concepts.

## Verified workflows

| Area / routes | Actions and observed result | Evidence |
| --- | --- | --- |
| Landing `/` and section links | Primary/sample/plan CTAs navigate; Standard intent survives the auth link. Mobile menu opens, navigates and closes. Invoice Document/Results tabs and all five use-case tabs show their corresponding content. Calculator 500×3 gives 25 hours and 1200×5 gives 100 hours. FAQ expands. Desktop and mobile lower sections and tablet hero rendered without page overflow. | `evidence/landing-*.png` |
| Help `/help`, `/help/api`, `/privacy`, `/terms` | All routes load from actual navigation links. At this historical check, Help/API described `/api` and the then-missing AI adapter. Legal pages explicitly described a local development preview. Tablet help and mobile API/privacy/terms had document width equal to viewport width. Later AI copy is not proven by these older captures. | `help-tablet.png`, `api-help-mobile.png`, `privacy-mobile.png` |
| Authentication `/sign-up`, `/sign-in` | Owner registered through UI; persisted workspace/session survive reload. Logout completes. Incorrect password returns the specific invalid-credentials message; correct password logs in. A separate locally registered viewer account logs in independently. | `onboarding-desktop.png`, `workspace-isolation.png` |
| Parser onboarding/list `/app/parsers/new`, `/app/parsers`, `/app/parsers/:id` | Created invoice and receipt parsers in the same workspace. Sample buttons send originals through intake/worker. Invoice parser rename persists; archive removes it from Active, Archived shows it, Restore returns it. | `onboarding-desktop.png`; durable local parser records |
| Schema/templates | Saved schema v2 with Department default Operations and reordered it before line items. Created a matching Juniper template. Saved schema v3 with Department choices Operations/Finance; reprocessing used v3 and a native choice selector; correction to Finance survives reload. New schema versions preserve earlier runs. Compact editor refinement receives a separate final recapture below. | `schema-desktop.png`, `schema-choices-desktop.png`, `review-choices-desktop.png` |
| Intake/documents `/app`, parser Documents tab | Browser file chooser batch accepted a real two-page PDF while rejecting malformed.pdf. Queued state was visible and completed through the worker. Search and status filters show correct empty states. Parser-specific list and global list show stored originals. Selection, bulk reprocessing and confirmed deletion of the disposable receipt sample work; the scan remains. | `document-queued-desktop.png`, `documents-desktop.png`, `documents-mobile.png`, `bulk-deletion-desktop.png` |
| Review `/app/documents/:id` | Original text and two native PDF canvases render. Page 1/Page 2, source evidence, zoom and mobile Source/Fields switch work. Invoice values and four PDF line items match the original. Supplier and lineitem correction save. Clearing a required invoice ID allows a draft but blocks approval with a specific validation error; restoring it permits approval. History retains corrections and approvals. Reprocessing adds a run without replacing the selected prior run or its approved snapshot. | `review-pdf-desktop.png`, `review-pdf-page2.png`, `review-line-items-desktop.png`, `review-history-desktop.png`, `review-mobile.png`, `review-tablet.png`, `review-source-mobile.png` |
| Historical unconfigured scan failure | Accepted synthetic PNG renders as the original; processing reports that readable text is absent and OCR/AI provider configuration is required. Export is disabled. No fabricated extraction is displayed. This remains evidence for the unconfigured path, not the later configured AI path. | `scan-provider-required.png` |
| Export | Real CSV, XLSX and JSON download bytes match saved approved snapshots. Corrected supplier/lineitem values and original identifier are preserved; XLSX amounts are numeric. Downloaded PDF bytes match uploaded fixture. Custom mapping Invoice/Vendor/Amount was reordered, saved as Accounts review, reloaded, selected and downloaded with exactly those headings and approved values. Mapping modal fits 390px. | `browser-downloads.json`, `approved-sample.csv`, `.xlsx`, `.json`, `export-mapping-desktop.png`, `export-mapping-mobile.png`; custom mapped download copied below |
| Workspace/settings `/app/settings` | Workspace renamed and retention 90 days/preference persist after reload. Manual invitation created and accepted by the second account. Scoped read-only API key created then revoked through UI. No invitation email was sent. | `settings-workspace-desktop.png` |
| Isolation and roles | Second account's own workspace cannot load the owner's document. After accepting a viewer invitation, it can read and export already approved output, while field editing, correction, approval and reprocess controls remain disabled. Switching between its own empty workspace and shared workspace changes data correctly. | `workspace-isolation.png`, `viewer-review.png` |
| Integration/usage/billing `/app/integrations`, `/app/usage` | Unconfigured provider status is explicit. No invented receiving address; Google/Stripe dependent controls remain unavailable. Invalid internal HTTP webhook URL is rejected before delivery. Usage reflects upload/reprocess page events and zero model cost. Billing states local allowance and disabled external checkout. | `integrations-desktop.png`, `usage-desktop.png`, `billing-desktop.png` |
| Responsive navigation/accessibility | Mobile sidebar controls are hidden from the native accessibility tree when closed; open drawer traps focus, Escape closes it and returns focus. Resize from open mobile drawer to desktop clears modal/inert state and Signout remains usable. Dialogs use native labelled controls. PDF/table overflow is contained inside its pane; checked pages have no document-level horizontal overflow. | `review-tablet.png`, `review-mobile.png`, `export-mapping-mobile.png` |

Artifact names in the last column are relative to `docs/evidence/` unless already prefixed. Screenshots record a point in time: older screenshots may use the original parser name Supplier invoices or show earlier schema versions. Newer screenshots and persisted records document later corrections.

## Specific corrections from browser testing

- Raised the landing header above illustration bounds, corrected desktop pricing color/heading wraps and aligned hero scale with the concept.
- Normalized field-key order when comparing draft and baseline values, eliminating a false Unsaved label on a fresh review.
- Pinned the selected extraction run while a reprocess completes, preserving the current reviewed run/draft.
- Changed tablet navigation to a drawer, removed stale sidebar inert state on resize, and made the narrow review form a single field column.
- Fixed the two internal links that incorrectly targeted `/app/documents` rather than `/app`.
- Added usable custom column ordering/saving and allowed-choice field editing; corrected singular delete wording.

## Scope and remaining acceptance limits

The browser exercised the central shipped routes and the workflows above, not every possible value combination or large-tenant performance. Concurrent queue/retry delivery, export escaping, selected nested structures and role enforcement additionally have separate automated evidence. Pagination is established by the explicit browser continuation below; it was not covered by the earlier automated suite. No real provider or deployment is inferred from local UI success. Final compact-schema and processing-notification checks are appended below. Development hot reloads during edits are not production behavior.


## Final schema refinement

The replacement desktop capture `evidence/schema-compact-desktop.png` shows five full core rows at 1536×1024. Root directly compared it with `design/concepts/10-app-schema.png` using `view_image`: the concept shows six rows and four nested children, so the implementation remains less dense. Opening Department exposes the saved Operations/Finance choices and Operations default. Final `schema-options-mobile.png` confirms 14px text, 44px native input height and document width 390px at a 390px viewport.

A nested `line_items.item_meta.cost_centre` field was created through the UI, saved as schema v4 and confirmed after reload. It was then removed and saved as v5, preserving both immutable versions. This establishes nested schema configuration and persistence; it is not a claim that text anchors can recover arbitrary nested structures from an unconfigured document. `schema-nested-desktop.png` records the nested editor. The saved Juniper template was disabled and enabled again through its controls.

The custom mapping download is retained as `evidence/approved-sample-mapped.csv`; exact headings are Invoice, Vendor and Amount, and the single record is INV-00127 / Juniper Studio — accounts / 553.5. This deliberately uses the earlier approved revision while a later run still needs review.

## Processing notifications

After migrations 005/006, the saved off preference produced an explicit disabled inbox. Its Notification settings link opened the correct tab. Enabling the preference showed five real terminal jobs in this workspace, including the failed scan and completed invoice runs. Marking the scan read reduced the count to four; reload preserved that read state. Mark all read then reload produced zero unread with the action disabled. The document link opened the corresponding PDF review. Deleted receipt jobs did not appear.

At 390×844 the dialog fits inside the viewport without page overflow (`evidence/notifications-mobile.png`). Escape closes the dialog and returns focus to the Processing notifications button. `notifications-desktop.png` shows the persisted zero-unread state at 1536×1024. These are in-app outcomes, with no notification email delivery. Per-member isolation and configured-age deletion are also checked by the separate automated suite.

`review-desktop-final.png` is a fresh native two-page PDF review with the corrected Extracted values status and new notification bell; root inspected it using `view_image`. The older `review-desktop.png` is retained as historical evidence of the false-unsaved bug before its fix.

The local password form also accepted a change for the synthetic owner account while retaining its current session. A second change authenticated using the temporary password and restored the original fixture password. The UI reported that other sessions were signed out; independent browser sessions were not opened for this last password-specific check. Workspace Activity renders the actual parser/document/export/member events; no password audit entry was observed.

Retained download identities are in `evidence/browser-artifact-hashes.json`. The evaluation generator later refreshed binary fixture metadata, so `browser-original-invoice.pdf` preserves the PDF bytes actually uploaded and downloaded during the earlier browser check; it should not be compared byte-for-byte with a newly regenerated fixture. The structured extraction contents remain separately asserted.

## Final decoder browser smoke

After the complete 63-test run and decoder/reconciliation changes, the separately running worker was restarted. Browser upload of the held-out `receipt.docx` entered the real intake, isolated decoder and worker, then produced Rowan Kitchen / 2026-08-23 / GBP / 21.4, matching all four expected values. Approval persisted after reload as Run 1 · Approved / Processed and enabled Export. Evidence: `review-docx-final.png` and `review-docx-approved.png`. This does not add AI/OCR evidence.

The final browser development-log read returned an empty error/warning list after approval/reload. The viewport override was reset and the working application tab marked as a deliverable. API, Vite and the background worker were left running locally.

## Goal continuation: pagination and remaining administration controls

The completion audit found that the earlier single-page list check did not establish Next/Previous behavior. An isolated Pagination QA receipt parser received 26 real synthetic TXT originals in two browser file-chooser batches (20 and 6), producing 26 distinct list rows across pages of 12, 12 and 2. Root confirmed all 26 filenames were unique across the pages. Selecting all on page 1 selected only that page; Next cleared the selection. Next was disabled on page 3; Previous returned to page 2. Search for pagination-26 reset to page 1 and returned one document. Failed status returned an empty state and disabled export.

Deleting the two selected final-page originals produced 24 documents on page 2. Deleting its 12 selected originals produced 12 documents on page 1. Deleting those 12 produced an empty parser. All mutations affected the explicitly generated disposable fixtures; the pre-existing invoice, PDF, scan and DOCX records remain separate. Fixture source/manifest: `fixtures/browser-pagination/`. Evidence: `pagination-page2.png`, `pagination-after-last-page-delete.png`. This is a three-page boundary check, not a large-load benchmark.

The disposable parser also exercised template creation then deletion. In Members, the synthetic Casey Viewer membership changed to Editor and survived reload, then returned to Viewer and was removed through the confirmation dialog. The account itself and its separate workspace were retained. A new disposable invitation was created without email, its Copy action exercised, then revoked and dismissed; reload confirmed no pending invitations. `members-lifecycle.png` shows the resulting state without an active token. The earlier viewer acceptance screenshots remain historical evidence from before this deliberate membership removal.

Webhook management used the exact-scoped `scripts/browser-webhook-fixture.ts` while the separately running worker and global delivery processors were stopped. It created a disabled connection and a synthetic failed event labelled **UI fixture — no request sent**, with a reserved `.example` destination. Enable persisted as Active after reload; Pause restored Inactive. Replay changed the same event from Failed to Queued with zero attempts (`webhook-ui-replay.png`). Removing the connection through its confirmation dialog and reloading left both Connections and Delivery history empty. No outbound request was made. This proves the management controls and persisted state transitions; it does not prove public-endpoint creation, network delivery or a provider account integration.

Creating the multiline QA parser exposed a name-loss bug: choosing a preset after typing a custom parser name overwrote the custom text. `Onboarding.tsx` now preserves a nonblank edited name. Root entered **Preserved custom name QA**, selected Lead emails, and read back the exact unchanged input; clearing that input then selecting Receipts produced the Receipts fallback. This check did not submit another parser. The earlier lead parser was renamed **Multiline lead QA** through Settings and retained for the extraction smoke below.

## Multiline browser smoke after the 81-test suite

The restarted worker processed the real `fixtures/browser-multiline/lead-message.txt` upload in **Multiline lead QA** (stock Lead emails schema v1). Message retains exactly three lines; the subsequent Company is independently Willow Research. The History tab records text-anchors / deterministic-v2 / folio-extraction-v1. `review-multiline.png` shows the source, separate fields and original evidence. The DOM capture returned an empty Email input value despite visible text; the screenshot and downloaded JSON independently confirm morgan@example.test. This discrepancy is retained explicitly in `multiline-browser-values.json`.

Approve & export saved the uncorrected revision. A real JSON download, retained as `approved-multiline.json`, matches every value in `fixtures/browser-multiline/expected.json`, including all message newlines. Reload showed Run 1 · Approved / Exported with export available (`review-multiline-approved.png`). Document ID: `46439fb9-7a2e-4973-b239-43dcad3ac8ac`; run ID: `a080e2c7-b953-47c7-bbb0-69f91c9c3b18`. No provider was invoked.

The final development browser error/warning list is empty (`browser-console-continuation.json`). API, Vite and the worker remain running locally.

## Additional format, settings and session acceptance

Pagination QA's Locale changed to de-DE and Timezone to Europe/Berlin through Settings. Save/reload preserved both. `parser-locale-settings.json` records their actual selected values and a 1280px document at a 1280px viewport; `parser-locale-settings.png` records the clearer hints. Locale affects day/month and decimal interpretation. Timezone is saved, but only calendar-date fields exist; timestamp/timezone conversion is not implemented. At this historical check, prose instructions were stored for then-unavailable AI mode; they do not change text-anchor behavior. The later configured AI adapter consumes pinned instructions, verified separately by controlled worker tests.

Root identified exactly one active session for the synthetic browser owner, expired only that session ID, then reloaded the current parser. Folio redirected to Sign in with the parser path preserved in the next parameter (`expired-session-sign-in.png`). Signing in restored Pagination QA. Document/job/run/approval/usage counts were identical across the expiration mutation; `browser-session-expiry.json` retains those counts and the exact synthetic session ID. No token was captured. The separate intake-policy suite checks 401 rejection and a fresh session independently.

The sender-restriction policy test creates the actual email.rejected audit metadata through a controlled valid envelope; the Activity UI now displays its bounded reason. Browser rendering used one explicitly labelled **UI fixture — Sender not on allowlist. No email received.** audit row, because the automated test's workspace was cleaned. `email-rejection-reason.png` shows the readable reason. That one fixture row was then removed by exact ID/workspace/entity/marker match (`email-rejection-ui-fixture.json`). All actual activity entries remain. This is rendering evidence, not live inbox delivery.

## Locale, HTML and lifecycle browser acceptance after the 107-test suite

The restarted worker processed a real upload of `fixtures/browser-locale/receipt-de.txt` in Pagination QA with its saved de-DE locale and Europe/Berlin timezone. The review showed Merchant **Linden Küche**, Date **2026-09-17**, Currency **EUR** and Total **1234.56**, while retaining source values **17.09.2026** and **1.234,56**. Approval used revision zero without corrections. A real downloaded JSON file, retained as `evidence/approved-locale-receipt.json`, exactly matches `fixtures/browser-locale/expected.json`. The download is 457 bytes, SHA-256 `8fd08d5dc44f4c93abbf0d4b16237463a11b487edfa0c1144ec0c61563323539`. Document `b4f22abc-ab4d-4fb8-bf67-05d687cd854e`, run `e0e69cf3-72d4-4795-9045-dd8dd4631435`, approval `52763c3c-c2d6-4736-a62e-c4eb5478d8bd` persist locally. This proves calendar-date and decimal normalization for this fixture, not timestamp conversion.

After reload, History showed seven real journal entries, newest first: Export completed, Export started, Approved, Ready for review, Processing, Queued and Received. Export entries explicitly identify the approved revision context and JSON format; selected-run provenance remains separate below. `evidence/lifecycle-browser.json`, `evidence/lifecycle-before-approval.png` and `evidence/lifecycle-approved-export.png` retain the desktop proof. An earlier document created before migration 008 correctly had no reconstructed lifecycle events while retaining its existing extraction and approval history. Synchronous export events become visible with the completed request; this browser check does not establish a live export queue or recovery from process loss during rendering.

The History view was also checked at 390×844. Document and body widths were exactly 390px. Actual scrolling exposed the remaining processing, queued and received entries, selected-run provenance and the approval disclosure; the bottom action controls remained accessible. Use `evidence/lifecycle-mobile.png` and `evidence/lifecycle-mobile-scrolled.png` for the two viewport captures, and the corresponding JSON files for DOM measurements. A full-page mobile screenshot had a compositor stitching artifact, so the retained canonical mobile screenshot is the viewport capture. The temporary viewport override was reset afterward.

A separate real upload of `fixtures/source-formats/receipt-table.html` in Receipt QA produced source text with preserved cell and row boundaries: `Merchant | Total`, then `Cedar & Pine | 18.60`, then `Currency: EUR`. Merchant **Cedar & Pine**, Total **18.6**, and Currency **EUR** extracted correctly. Date stayed empty and **Date is required** kept the document in Needs review; it was not approved. Document `7aac2995-706e-4ea6-89b0-0bcb35a83db5` remains available for inspection. `evidence/html-browser-values.json` and `evidence/review-html-table.png` retain the evidence. Broader HTML layout/OCR fidelity is outside this fixture's scope.

After returning to the approved locale receipt at the normal 1280×720 viewport, the captured browser error/warning list was empty (`evidence/browser-console-lifecycle.json`). The tab is retained as a deliverable; API, Vite, PostgreSQL and the worker remain running locally. These checks used synthetic data and the deterministic engine, with no external provider call.

## Historical configured AI preparation — 7 September 2026

At this historical checkpoint, secure OpenAI setup and the native adapter were complete. [The prepared form](evidence/ai-parser-pending-approval.png) shows the configured AI option, parser name **AI receipt QA**, and **Receipt** preset selected. Automatic approval review rejected the parser **Create** action. The form had not been submitted; parser creation, upload, AI review, approval, export and reload had **not** been completed through that browser workflow. Continuing required explicit approval for the rejected action. No browser AI success is inferred from the form, configuration flag or separate API calls.

Separate backend evidence at that checkpoint: [145 tests](evidence/openai-date-tests-results.txt), [build](evidence/openai-date-build-results.txt), [180-file integrity](evidence/openai-date-final-integrity.json), the original [49/51 held-out evaluation](evidence/openai-heldout-evaluation-2026-09-07T08-30-01-014Z.json) and [51/51 regression replay](evidence/openai-date-regression-evaluation-2026-09-07T08-34-10-311Z.json) after date selection was corrected in prompt v2. The held-out failure and earlier v1 74/74 regression remain preserved. These evaluations include a repeated identical image and do not establish general accuracy or replace the pending browser flow. Visual/missing-evidence issues remain in the reports.

[Historical local runtime evidence](evidence/openai-date-local-runtime.json) records HTTP 200 from web, API and presets with AI configured after restoring the v2 worker. Startup was observed after tests; the preceding database read showed eight completed and one failed job, with no queued/processing jobs. The prepared AI form remained unsubmitted at that checkpoint. Resend, Sheets, Stripe provider flows and production remain separate gates; the native goal was active at that checkpoint. The goal later became blocked pending Mac unlock and explicit AI workflow approval; both were subsequently resolved by the user, and the completed workflow is recorded below.

## Historical passive AI controls and locked-Mac checkpoint

The prepared AI receipt form was inspected at 390×844; document/body width were both 390px, the configured AI option and receipt preset remained selected, and the form was not submitted. [Mobile form](evidence/ai-onboarding-mobile-pending.png). A separate read-only Usage tab preserved the unsaved form; its actual size was 1280×720 and document/body width were both 1280px. It showed Estimated model cost and the recorded-successful-run/billing limitation. [Desktop Usage](evidence/ai-usage-desktop-readonly.png). The latter is desktop evidence, not a completed mobile Usage check. [Measurements and screenshot hashes](evidence/ai-passive-browser-check.json).

At that checkpoint CUA reported that the Mac was locked and automatic unlock failed. Browser operations stopped until the user subsequently unlocked it. Unlock and temporary-viewport reset were pending at that checkpoint. The subsequent table/nested-field source-disclosure fix has eight component tests, plus seven availability/cost rendering tests, within the passing **160-test aggregate** and build. [REVIEW-PROVENANCE.md](REVIEW-PROVENANCE.md) records exact scope and source identity. Its rendered browser layout, keyboard/focus, page navigation and console checks remained pending then. The actual AI create/upload/review/approve/download flow separately needed explicit approval for the rejected parser submission. No browser AI success was claimed at that checkpoint; the completed checks are recorded below.


## AI receipt and review-source browser completion — 7 September 2026

After the user unlocked the Mac and explicitly approved creation, **AI receipt QA** was created using the Receipt preset and AI mode. The real uploaded PNG completed worker extraction, displayed Birch Bakery / 2026-08-25 / EUR / 18.6 with original total 18.60, and retained AI-read warnings. All four values were compared with the original image before approving revision zero without correction. The actual downloaded JSON matched, and reload preserved Approved / Exported plus all seven lifecycle entries. [AI-BROWSER-ACCEPTANCE.md](AI-BROWSER-ACCEPTANCE.md) records IDs, bytes/hash, model/prompt/tokens, usage and direct DOM/screenshots. The earlier prepared-state rejection and Mac lock are historical.

Browser verification passed at **1280×720** and **390×844**. Desktop document/body widths were 1280px; after the targeted CSS fix mobile widths were both 390px, compared with the retained earlier 422px document overflow. Source buttons were at least 44px high. Native disclosure Space toggling, Tab focus and Enter activation passed. Page-two quotations navigated to page two; mobile source activation selected the Document tab, with page-one navigation checked too. The original four row labels and complete table-level quotations were present, while absent leaf quotations remained explicit. A temporary unsaved Description edit left the original disclosure text unchanged and was restored without saving. Nested objects, deeper recursion and structural row edits retain controlled component evidence; this browser pass did not create a fixture for every depth. The final browser warning/error read was empty and the temporary viewport was reset.

Direct source-disclosure evidence: [desktop DOM](evidence/source-disclosure-desktop.txt), [desktop screenshot](evidence/source-disclosure-desktop.png), [before-fix measurements](evidence/source-disclosure-mobile-before-fix.json), [before-fix screenshot](evidence/source-disclosure-mobile.png), [after-fix measurements](evidence/source-disclosure-mobile-after-fix.json), [after-fix screenshot](evidence/source-disclosure-mobile-after-fix.png), [mobile page-two source](evidence/source-disclosure-mobile-page2.txt), [page-two screenshot](evidence/source-disclosure-mobile-page2.png), [mobile Document DOM](evidence/source-disclosure-mobile-document.txt) and [Document screenshot](evidence/source-disclosure-mobile-document.png). The [AI mobile fields](evidence/ai-browser-mobile.png) and [original image](evidence/ai-browser-mobile-original.png) also fit 390px; activating the Total source button switched to Document. [Consolidated acceptance](evidence/ai-browser-acceptance.json) includes final console and viewport-reset observations.

The final source passed **160/160 tests**, zero failures/skips, from `2026-09-07T17:15:22.744417Z` to `17:16:03.468224Z`; TypeScript/Vite build passed from `17:16:03.469913Z` to `17:16:05.141937Z`. [Test output](evidence/ai-browser-tests-results.txt), [test timing](evidence/ai-browser-tests-run.json), [build output](evidence/ai-browser-build-results.txt) and [build timing](evidence/ai-browser-build-run.json) are preserved. The [final integrity record](evidence/ai-browser-final-integrity.json) identifies **185 files**, fingerprint `d58b8bbac1a945458f955b204da5499273de6ef12531636d809ca11f27f736c4`, unchanged during verification. Only `src/features/documents/review.css` changed from the preceding source-disclosure checkpoint: its table wrapper now contains the absolutely positioned screen-reader label, eliminating the observed mobile page overflow.


After the final test/build run, the API, worker and Vite services were restarted. Browser reload again showed **Run 1 · Approved**, **Exported** and the same four values. The final 1280×720 viewport had a 1280px document width and no captured browser warning/error entries. [After-restart DOM](evidence/ai-browser-after-restart.txt), [screenshot](evidence/ai-browser-after-restart.png), [acceptance record](evidence/ai-browser-acceptance.json).

The current [redacted runtime/configuration check](evidence/ai-browser-runtime.json), recorded `2026-09-07T17:19:42.405Z`, confirms web/API health/presets HTTP 200, observed worker startup and OpenAI configuration. Required Stripe test, Resend and Google OAuth settings remain absent, with Resend inbound disabled. No credential values were printed.
